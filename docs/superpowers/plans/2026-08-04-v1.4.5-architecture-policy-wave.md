# v1.4.5 Architecture Policy Wave Implementation Plan

> **For agentic workers:** Documentation-only wave. Read the v1.4.5 handwritten contract before editing. All prose is handwritten and source-backed; do not use body generators.

**Goal:** Complete the missing v1.4.5 architecture policy surface so a modder can choose a task route, classify non-business types, and decide how to read SandBox, StoryMode, and Native.

**Architecture:** Add the three missing bilingual policy pages under the existing v1.4.5 architecture section. Preserve all existing URLs and long-tail catalogs, then add reciprocal curated routes in `data/navigation.json`; regenerate only structural navigation outputs. The pages link back to the architecture/API hubs and forward to existing task anchors.

**Tech Stack:** Markdown, Zola routes, JSON navigation data, Node.js structural and audit scripts.

## Global Constraints

- Product prose under `content/**` remains handwritten; no signature-to-prose or body-generation tool may run.
- v1.4.5 source is authoritative; v1.3.15 pages are a writing reference, not an API claim.
- Preserve all unrelated dirty worktree changes; do not reset, clean, stash, or commit.
- Relative links must resolve; `BROKEN_LINKS=0` is required.
- R1 coverage remains incomplete while its gap is nonzero.

### Task 1: Author the missing policy pages

**Files:**
- Create: `content/v1.4.5/zh/architecture/developer-roadmap.md`
- Create: `content/v1.4.5/en/architecture/developer-roadmap.md`
- Create: `content/v1.4.5/zh/architecture/noise-policy.md`
- Create: `content/v1.4.5/en/architecture/noise-policy.md`
- Create: `content/v1.4.5/zh/architecture/sandbox-native-policy.md`
- Create: `content/v1.4.5/en/architecture/sandbox-native-policy.md`

**Content contract:** Each page has informative frontmatter, a mental model, task timing or classification rules, real existing API paths, risks, and parent/sibling/related links. The roadmap must answer the five required scenarios: SubModule/Behavior registration, Action-backed world mutation, custom save data, MissionBehavior/Agent lifetime, and Model versus Action. The noise page must explain AutoGenerated/third-party/platform bridge handling and its managed fallback. The SandBox/StoryMode/Native page must distinguish reference implementation, story content, and engine reverse-engineering boundaries.

**QA:** Manually inspect every new page for the contract sections and real links; reject placeholders, fake acquisition assignments, signature-only prose, or v1.3.15-only claims.

### Task 2: Connect the architecture tree

**Files:**
- Modify: `content/v1.4.5/zh/architecture/_index.md`
- Modify: `content/v1.4.5/en/architecture/_index.md`
- Modify: `data/navigation.json`
- Regenerate: `data/section-tree.json`, `data/page-navigation.json`

**Content contract:** Add the three pages to the architecture child table and relevant see-also links. Add both language routes to the curated navigation with stable labels, parent `/v1.4.5/<lang>/architecture/`, explicit children where needed, and no duplicate route ownership.

**QA:** `node tools/audit-navigation.mjs`, `node tools/generate-section-tree.mjs --check`, and `node tools/generate-page-navigation.mjs --check` must pass; the new routes must appear in the generated tree and retain parent navigation.

### Task 3: Evidence and regression gates

**Files:**
- Update: `.tmp/manual-scenario-acceptance-2026-08-04.md` only with this run's evidence if the existing record is present.
- Update: `tools/data/*` only when a report command explicitly regenerates a stale report.

**Commands:**
- `npm test`
- `node tools/audit-links.mjs`
- `node tools/audit-body-writers.mjs`
- `node tools/audit-doc-quality.mjs`
- `node tools/audit-navigation.mjs`
- `node tools/generate-section-tree.mjs --check`
- `node tools/generate-page-navigation.mjs --check`
- v1.4.5 zh/en handwritten and R1 report checks without claiming zero gaps
- `npm run docs:build` independently; record the R: canonical-path failure if reproduced

**Success criteria:** All structural gates exit 0, `BROKEN_LINKS=0`, no new quality blocker appears, six pages are classified as non-stub architecture pages, and the evidence records that R1/H10 is still incomplete.
