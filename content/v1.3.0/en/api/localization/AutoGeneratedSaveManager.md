---
title: "AutoGeneratedSaveManager"
description: "Auto-generated class reference for AutoGeneratedSaveManager."
---
# AutoGeneratedSaveManager

**Namespace:** TaleWorlds.Localization
**Module:** TaleWorlds.Localization
**Type:** `internal class AutoGeneratedSaveManager : IAutoGeneratedSaveManager`
**Base:** `IAutoGeneratedSaveManager`
**File:** `TaleWorlds.Localization/AutoGeneratedSaveManager.cs`

## Overview

`AutoGeneratedSaveManager` is a manager: it owns a subsystem's lifecycle, lookup entry points, and cross-object coordination responsibilities.

## Mental Model

Treat `AutoGeneratedSaveManager` as a Manager-style extension point: first identify who creates it, who owns it, and who calls it, then decide whether you should subclass it, compose it, or only read from it.

## Key Methods

### Initialize
`public void Initialize(DefinitionContext definitionContext)`

**Purpose:** Prepares the resources, state, or bindings the this instance needs before use.

```csharp
// Obtain an instance of AutoGeneratedSaveManager from the subsystem API first
AutoGeneratedSaveManager autoGeneratedSaveManager = ...;
autoGeneratedSaveManager.Initialize(definitionContext);
```

## Usage Example

```csharp
var manager = AutoGeneratedSaveManager.Current;
```

## See Also

- [Area Index](../)