# v1.4.5 Settlement and Culture Handwritten Wave Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Each task is a bilingual handwritten documentation rewrite followed by an independent review.

**Goal:** Replace four remaining v1.4.5 S-tier campaign entity stubs with source-backed bilingual handbook pages that explain real acquisition paths, mutation boundaries, dependencies, and failure risks.

**Architecture:** Rewrite the existing leaf URLs in place. Each type gets one Chinese and one English page written from the 1.4.5 source and call sites; the two language pages must express the same lifecycle and safety model. Existing navigation and generated reports remain authoritative and may only be refreshed after content and link checks.

**Tech Stack:** Markdown, Zola clean routes, Bannerlord 1.4.5 decompiled C# source, Node.js handwritten-policy, link, quality, navigation, and coverage audits.

## Global Constraints

- All business-class body prose is handwritten after reading source and call sites; signatures are evidence, never the product explanation.
- Do not invoke retired body generators, stub writers, or bulk prose mutators.
- Preserve the existing v1.4.5 URLs and bilingual structure; do not restore deleted generated stubs.
- Every page must contain a mental model, when/when-not guidance, real acquisition path, dependency links, meaningful member behavior, a real C# example, risk boundaries, version context, and bidirectional navigation.
- World-state mutation must point to the relevant Action or lifecycle owner; do not teach direct field writes as a safe replacement.
- All links are relative and must pass `BROKEN_LINKS=0`; all four type pairs must classify as `deep_pass`.
- Do not reset, clean, stash, checkout, commit, or discard unrelated dirty-worktree changes.

---

### Task 1: Town

**Files:**
- Modify: `content/v1.4.5/zh/api/campaign/Town.md`
- Modify: `content/v1.4.5/en/api/campaign/Town.md`

**Source evidence:** `bannerlord-1.4.5/Bannerlord.Source/bin/TaleWorlds.CampaignSystem/TaleWorlds.CampaignSystem.Settlements/Town.cs`; `Settlement.cs`; `Fief.cs`; `Village.cs`; `TownHelpers.cs`; settlement economy, siege, garrison, loyalty, security, and clan-management call sites.

**Required content:** Explain `Settlement.Town` acquisition and the `Town`/`Settlement`/`Fief` ownership boundary; town-wide state such as prosperity, food, loyalty, security, militia, walls, garrison, governor, workshops, and trade tax; which values are observations versus Action/Model-owned mutations; `AllFiefs` and map-object lifecycle; real examples for reading a current settlement and querying town calculations; save/load and rebellion/capture risks; links to `Village`, `Workshop`, `SettlementLoyaltyModel`, `SettlementSecurityModel`, `ChangeOwnerOfSettlementAction`, and `ChangeGovernorAction` where supported by source.

**QA:** Both pages classify `deep_pass`; no placeholder acquisition or direct unsafe mutation example; all links resolve; bilingual headings cover the same lifecycle and risk boundaries.

### Task 2: Village

**Files:**
- Modify: `content/v1.4.5/zh/api/campaign/Village.md`
- Modify: `content/v1.4.5/en/api/campaign/Village.md`

**Source evidence:** `bannerlord-1.4.5/Bannerlord.Source/bin/TaleWorlds.CampaignSystem/TaleWorlds.CampaignSystem.Settlements/Village.cs`; `Settlement.cs`; `Town.cs`; `VillageTradeModel`; `VillageProductionCalculator`; `VillageState`; bound-village and raid/caravan/gathering call sites.

**Required content:** Explain `Settlement.Village` acquisition, bound-town ownership, hearth and production state, `VillageState` transitions, bound parties and raids, militia/prosperity effects, read-only versus Action/Model boundaries, real examples using a current `Settlement`, and risks around captured/raided/removed villages and stale references. Link to `Town`, `Settlement`, `VillageTradeModel`, `VillageProductionCalculator`, and relevant event or Action pages.

**QA:** Both pages classify `deep_pass`; examples use real campaign acquisition paths; no invented setters or placeholder values; links and bilingual parity pass.

### Task 3: Workshop

**Files:**
- Modify: `content/v1.4.5/zh/api/campaign/Workshop.md`
- Modify: `content/v1.4.5/en/api/campaign/Workshop.md`

**Source evidence:** `bannerlord-1.4.5/Bannerlord.Source/bin/TaleWorlds.CampaignSystem/TaleWorlds.CampaignSystem.Settlements.Workshops/Workshop.cs`; `WorkshopType.cs`; `Settlement.cs`; `Clan.cs`; `ChangeOwnerOfWorkshopAction`; workshop income/production model call sites and clan-management UI call sites.

**Required content:** Explain workshop ownership, settlement placement, workshop type and production, `WorkshopType` lookup, `IsRunning`/profitability and income timing, owner-change/destroy boundaries, how to obtain a real workshop from a clan or settlement, and why direct owner or profit writes are unsafe. Include a real inspection example and links to `Clan`, `Settlement`, `WorkshopType`, `ChangeOwnerOfWorkshopAction`, `ClanFinanceModel`, and save/event dependencies where present in source.

**QA:** Both pages classify `deep_pass`; examples use real campaign objects and stable lookup paths; mutation warnings are explicit; links and bilingual parity pass.

### Task 4: CultureObject

**Files:**
- Modify: `content/v1.4.5/zh/api/campaign/CultureObject.md`
- Modify: `content/v1.4.5/en/api/campaign/CultureObject.md`

**Source evidence:** `bannerlord-1.4.5/Bannerlord.Source/bin/TaleWorlds.CampaignSystem/TaleWorlds.CampaignSystem.CultureObject.cs`; `MBObjectBase`; `MBObjectManager`; `Hero`, `Clan`, `Kingdom`, `CharacterObject`, `Settlement`, and culture-dependent model/call sites.

**Required content:** Explain culture as a registered campaign definition rather than a mutable faction/settlement owner; stable `StringId`/object-manager lifecycle; acquisition from registered objects and campaign data; culture flags, names, default heroes/troops and model inputs only where confirmed by source; mod initialization and lookup timing; save/load and duplicate-ID risks; and a real example that reads a culture from a current campaign object without pretending to create or mutate it at runtime. Link to `MBObjectManager`, `CharacterObject`, `Hero`, `Clan`, `Kingdom`, `Settlement`, and relevant culture/model pages.

**QA:** Both pages classify `deep_pass`; example uses a real registered object path; no invented runtime registration API; links resolve and bilingual parity passes.

### Task 5: Evidence gates

**Files:**
- Update: `.superpowers/sdd/2026-08-04-v1.4.5-settlement-culture-wave/progress.md`
- Refresh generated `data/` and `tools/data/` reports only when the corresponding checks prove them stale.

**Commands:**
- `npm test`
- `npm run audit:links`
- `node tools/audit-doc-quality.mjs content --scope v1.4.5`
- `npm run audit:body-writers`
- `npm run audit:navigation`
- `npm run audit:section-tree`
- `npm run audit:page-navigation`
- v1.4.5 zh/en handwritten and R1 coverage generation followed by `--check`
- `npm run docs:build` independently, recording the R: RamDisk result if it persists

**Success criteria:** Eight pages are `deep_pass`; S-tier coverage increases by four per language if identity metadata matches; all Node gates pass; `BROKEN_LINKS=0`; the ledger records review findings and exact counts. The global H0-H10 goal remains active while any R1 gap or required build/scenario evidence remains.
