---
title: "GOGLoginAccessProvider"
description: "Auto-generated class reference for GOGLoginAccessProvider."
---
# GOGLoginAccessProvider

**Namespace:** TaleWorlds.Diamond.AccessProvider.GOG
**Module:** TaleWorlds.Diamond
**Type:** `public class GOGLoginAccessProvider : ILoginAccessProvider`
**Base:** `ILoginAccessProvider`
**File:** `bin/TaleWorlds.Diamond.AccessProvider.GOG/TaleWorlds.Diamond.AccessProvider.GOG/GOGLoginAccessProvider.cs`

## Overview

`GOGLoginAccessProvider` lives in `TaleWorlds.Diamond.AccessProvider.GOG` and exposes the state, behavior, or workflow entry points of that subsystem to mod developers through its public members. Read its properties as “what state it owns” and its methods as “what actions it allows”.

## Mental Model

Start from namespace `TaleWorlds.Diamond.AccessProvider.GOG` to place it in the stack, then inspect its public methods: if it mainly exposes Get/Set members, it is likely a state object; if it centers on Create/Apply/Execute verbs, it behaves more like a service or workflow entry point.

## Usage Example

```csharp
// Obtain an instance from the relevant subsystem API
GOGLoginAccessProvider instance = ...;
```

## See Also

- [Area Index](../)