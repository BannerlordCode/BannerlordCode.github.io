# Campaign Helpers Wave Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace six v1.4.5 `TaleWorlds.CampaignSystem.Helpers` shells with bilingual, source-backed pages that explain their calculation, UI/state, text, and lifecycle boundaries.

**Architecture:** Keep the existing `api/system` routes and document the six static helpers as consumers of campaign entities and model/state services, not as persistent owners. `DistanceHelper` delegates navigation semantics to `Campaign.Current.Models.MapDistanceModel`; `TownHelpers` reads settlement parties and rosters; `CraftingHelper` crosses the `GameStateManager` boundary; `StringHelpers` and `DialogHelper` mutate text-variable state; `CharacterHelper` provides troop, equipment, face, and character-template utilities.

**Tech Stack:** Markdown, Zola, Node.js audits, PowerShell, Bannerlord v1.4.5 decompiled C# source.

## Global Constraints

- All body prose is handwritten from v1.4.5 source and real call sites; no signature-to-prose generator or bulk body writer.
- Preserve the dirty shared worktree; do not reset, clean, stash, checkout, commit, or overwrite unrelated changes.
- Modify only the twelve target pages plus this plan and the wave evidence ledger.
- Use real inputs and acquisition paths such as `Campaign.Current.Models.MapDistanceModel`, `Settlement.CurrentSettlement`, `PartyBase.MainParty.MemberRoster`, and `Game.Current.GameStateManager`.
- Keep clean relative links, bilingual semantic parity, explicit risks, and the global R1/build/scenario boundary.

---

### Task 1: Freeze source contract and page boundary

**Files:**
- Read: `bannerlord-1.4.5/Bannerlord.Source/bin/TaleWorlds.CampaignSystem/Helpers/{CharacterHelper,DialogHelper,DistanceHelper,CraftingHelper,TownHelpers,StringHelpers}.cs`
- Read: source call sites under `bannerlord-1.4.5/Bannerlord.Source/bin/TaleWorlds.CampaignSystem`
- Read: `AGENTS.md` and `content/v1.4.5/zh/architecture/doc-contract.md`

- [x] **Step 1: Verify all twelve routes and current dirty-tree boundary**
- [x] **Step 2: Record public method groups, ownership inputs, and call-site evidence**

### Task 2: Rewrite the six helper pages in both languages

**Files:**
- Modify: `content/v1.4.5/{zh,en}/api/system/{CharacterHelper,DialogHelper,DistanceHelper,CraftingHelper,TownHelpers,StringHelpers}.md`

- [x] **Step 1: Rewrite `DistanceHelper` and `TownHelpers`**
- [x] **Step 2: Rewrite `CraftingHelper` and `DialogHelper`**
- [x] **Step 3: Rewrite `StringHelpers` and `CharacterHelper`**
- [x] **Step 4: Add reciprocal dependency, risk, example, and navigation sections**

### Task 3: Run scoped verification

**Files:**
- Read/verify: the twelve target pages and existing generated navigation artifacts
- Create: `.superpowers/sdd/2026-08-04-v1.4.5-campaign-helpers-wave/progress.md`

- [x] **Step 1: Require twelve `deep_pass` classifications and zero forbidden-token hits**
- [x] **Step 2: Run target clean-link and serial structural gates without regenerating navigation**
- [x] **Step 3: Record exact evidence and remaining global R1/build/manual-scenario gaps**
