---
title: "FlattenedTroopRosterElement"
description: "Auto-generated class reference for FlattenedTroopRosterElement."
---
# FlattenedTroopRosterElement

**Namespace:** TaleWorlds.CampaignSystem.Roster
**Module:** TaleWorlds.CampaignSystem
**Type:** `public struct FlattenedTroopRosterElement : ISavedStruct`
**Base:** `ISavedStruct`
**File:** `bin/TaleWorlds.CampaignSystem/TaleWorlds.CampaignSystem.Roster/FlattenedTroopRosterElement.cs`

## Overview

`FlattenedTroopRosterElement` lives in `TaleWorlds.CampaignSystem.Roster` and exposes the state, behavior, or workflow entry points of that subsystem to mod developers through its public members. Read its properties as “what state it owns” and its methods as “what actions it allows”.

## Mental Model

Start from namespace `TaleWorlds.CampaignSystem.Roster` to place it in the stack, then inspect its public methods: if it mainly exposes Get/Set members, it is likely a state object; if it centers on Create/Apply/Execute verbs, it behaves more like a service or workflow entry point.

## Key Properties

| Name | Signature |
|------|-----------|
| `State` | `public RosterTroopState State { get; }` |
| `IsWounded` | `public bool IsWounded { get; }` |
| `IsRouted` | `public bool IsRouted { get; set; }` |
| `IsKilled` | `public bool IsKilled { get; set; }` |

## Key Methods

### AutoGeneratedStaticCollectObjectsFlattenedTroopRosterElement
`public static void AutoGeneratedStaticCollectObjectsFlattenedTroopRosterElement(object o, List<object> collectedObjects)`

**Purpose:** **Purpose:** Executes the AutoGeneratedStaticCollectObjectsFlattenedTroopRosterElement logic.

```csharp
// Static call; no instance required
FlattenedTroopRosterElement.AutoGeneratedStaticCollectObjectsFlattenedTroopRosterElement(o, collectedObjects);
```

### ToString
`public override string ToString()`

**Purpose:** **Purpose:** Returns a human-readable string representation of the this instance.

```csharp
// Obtain an instance of FlattenedTroopRosterElement from the subsystem API first
FlattenedTroopRosterElement flattenedTroopRosterElement = ...;
var result = flattenedTroopRosterElement.ToString();
```

## Usage Example

```csharp
FlattenedTroopRosterElement.AutoGeneratedStaticCollectObjectsFlattenedTroopRosterElement(o, collectedObjects);
```

## See Also

- [Area Index](../)