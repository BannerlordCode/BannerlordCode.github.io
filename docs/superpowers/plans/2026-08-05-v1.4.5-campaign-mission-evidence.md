# CampaignMission Evidence Review Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:verification-before-completion when closing this review. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reconfirm the already-written v1.4.5 CampaignMission lifecycle family against one stable source/page snapshot and leave a reproducible evidence ledger without expanding scope.

**Architecture:** Treat `CampaignMission` and its two interfaces as the CampaignSystem contract, `CampaignMissionManager` as the SandBox forwarding adapter, and `CampaignMissionComponent` as the live Mission-owned implementation. This review only fingerprints those existing pages and source files, runs the repository's bounded structural gates, and records the mapped-drive build limitation separately.

**Tech Stack:** Markdown, PowerShell, Node.js repository audits, and Bannerlord v1.4.5 C# source.

## Global Constraints

- Keep the global goal active; this bounded review cannot prove global R1/H10 completion.
- Preserve all unrelated dirty-worktree changes; do not reset, clean, stash, checkout, commit, or push.
- Do not start authors, reviewers, or explorers, and do not hand-edit generated navigation artifacts.
- Treat source SHA-256 hashes, page SHA-256 hashes, exact command output, and the observed HEAD as one evidence bundle.
- Report `BROKEN_LINKS`, quality blockers, body-writer status, structural checks, inventory, and `npm test` independently.
- Report `npm run docs:build` as environment-blocked when Zola cannot canonicalize mapped `R:`; never convert that failure into a build pass.

### Task 1: Freeze the bounded scope

**Files:**
- Read: `content/v1.4.5/{en,zh}/api/campaign/{CampaignMission,ICampaignMission,ICampaignMissionManager}.md`
- Read: `content/v1.4.5/{en,zh}/api/campaign-ext/{CampaignMissionManager,CampaignMissionComponent}.md`
- Read: `bannerlord-1.4.5/Bannerlord.Source` lifecycle source files named in the progress ledger.

- [ ] Confirm the ten pages exist and remain outside the local diff.
- [ ] Confirm the source chain remains `Campaign.Current.CampaignMissionManager` -> SandBox adapter -> `SandBoxMissions` -> `CampaignMissionComponent` -> `CampaignMission.Current`.
- [ ] QA: record the HEAD, status summary, page hashes, and source hashes.

### Task 2: Run bounded repository gates

**Files:**
- Read: `package.json` and the existing audit scripts.
- Generate/check: existing report artifacts only when the command explicitly targets the current v1.4.5 language and a unique temporary output.

- [ ] Run `npm run audit:body-writers`.
- [ ] Run the v1.4.5 quality, link, navigation, section-tree, page-navigation, inventory, and test gates serially.
- [ ] Run target-only banned-pattern checks for the ten pages.
- [ ] QA: preserve exact pass/fail tokens and warning counts in the progress ledger.

### Task 3: Record the boundary

**Files:**
- Create: `.superpowers/sdd/2026-08-05-v1.4.5-campaign-mission-evidence/progress.md`

- [ ] Record source/page/report fingerprints and the date/time of the snapshot.
- [ ] Record bounded-wave results separately from global R1/H10, rendered build, and runtime/manual verification.
- [ ] Keep the global goal active because the repository still has a nonzero R1 gap and the mapped `R:` build has no supported success proof.
