---
title: "CampaignVec2"
description: "CampaignVec2 的自动生成类参考。"
---
# CampaignVec2

**Namespace:** TaleWorlds.CampaignSystem
**Module:** TaleWorlds.CampaignSystem
**Type:** `public struct CampaignVec2 : ISavedStruct`
**Base:** `ISavedStruct`
**File:** `bin/TaleWorlds.CampaignSystem/TaleWorlds.CampaignSystem/CampaignVec2.cs`

## 概述

`CampaignVec2` 位于 `TaleWorlds.CampaignSystem`，它通过这组公开成员把对应子系统的状态、行为或流程入口暴露给 mod 开发者。阅读时先看属性代表“它持有什么状态”，再看方法代表“它允许你做什么”。

## 心智模型

先从命名空间 `TaleWorlds.CampaignSystem` 判断它属于哪层系统，再看公开方法：如果以 Get/Set 为主，它多半是状态对象；如果以 Create/Apply/Execute 为主，它更像服务或流程入口。

## 主要属性

| Name | Signature |
|------|-----------|
| `Face` | `public PathFaceRecord Face { get; }` |

## 主要方法

### AutoGeneratedStaticCollectObjectsCampaignVec2
`public static void AutoGeneratedStaticCollectObjectsCampaignVec2(object o, List<object> collectedObjects)`

**用途 / Purpose:** 处理与 「auto generated static collect objects campaign vec2」 相关的逻辑。

```csharp
// 静态调用，不需要实例
CampaignVec2.AutoGeneratedStaticCollectObjectsCampaignVec2(o, collectedObjects);
```

### AddVec2
`public void AddVec2(Vec2 vec2)`

**用途 / Purpose:** 将 「vec2」 添加到当前容器或状态中。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
campaignVec2.AddVec2(vec2);
```

### ToVec2
`public Vec2 ToVec2()`

**用途 / Purpose:** 处理与 「to vec2」 相关的逻辑。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.ToVec2();
```

### Equals
`public override bool Equals(object obj)`

**用途 / Purpose:** 比较当前对象与传入实例是否相等。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.Equals(obj);
```

### AsVec3
`public Vec3 AsVec3()`

**用途 / Purpose:** 处理与 「as vec3」 相关的逻辑。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.AsVec3();
```

### Equals
`public bool Equals(CampaignVec2 other)`

**用途 / Purpose:** 比较当前对象与传入实例是否相等。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.Equals(other);
```

### IsValid
`public bool IsValid()`

**用途 / Purpose:** 判断当前对象是否处于 「valid」 状态或条件。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.IsValid();
```

### DistanceSquared
`public float DistanceSquared(CampaignVec2 v)`

**用途 / Purpose:** 处理与 「distance squared」 相关的逻辑。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.DistanceSquared(v);
```

### DistanceSquared
`public float DistanceSquared(Vec2 v)`

**用途 / Purpose:** 处理与 「distance squared」 相关的逻辑。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.DistanceSquared(v);
```

### Distance
`public float Distance(CampaignVec2 v)`

**用途 / Purpose:** 处理与 「distance」 相关的逻辑。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.Distance(v);
```

### Distance
`public float Distance(Vec2 v)`

**用途 / Purpose:** 处理与 「distance」 相关的逻辑。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.Distance(v);
```

### ToString
`public override string ToString()`

**用途 / Purpose:** 返回当前对象的人类可读字符串表示。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.ToString();
```

### IsDefault
`public bool IsDefault()`

**用途 / Purpose:** 判断当前对象是否处于 「default」 状态或条件。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.IsDefault();
```

### IsNonZero
`public bool IsNonZero()`

**用途 / Purpose:** 判断当前对象是否处于 「non zero」 状态或条件。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.IsNonZero();
```

### NearlyEquals
`public bool NearlyEquals(Vec2 v, float epsilon = 1E-05f)`

**用途 / Purpose:** 处理与 「nearly equals」 相关的逻辑。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.NearlyEquals(v, 0);
```

### NearlyEquals
`public bool NearlyEquals(CampaignVec2 v, float epsilon = 1E-05f)`

**用途 / Purpose:** 处理与 「nearly equals」 相关的逻辑。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.NearlyEquals(v, 0);
```

### Normalize
`public float Normalize()`

**用途 / Purpose:** 将当前对象规范化为标准形式或范围。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.Normalize();
```

### Normalized
`public static CampaignVec2 Normalized(CampaignVec2 v)`

**用途 / Purpose:** 将「d」规范化到标准形式或范围内。

```csharp
// 静态调用，不需要实例
CampaignVec2.Normalized(v);
```

### RightVec
`public CampaignVec2 RightVec()`

**用途 / Purpose:** 处理与 「right vec」 相关的逻辑。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.RightVec();
```

### LeftVec
`public CampaignVec2 LeftVec()`

**用途 / Purpose:** 处理与 「left vec」 相关的逻辑。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.LeftVec();
```

### GetHashCode
`public override int GetHashCode()`

**用途 / Purpose:** 返回当前对象的哈希码，用于字典或哈希集合中的快速查找。

```csharp
// 先通过子系统 API 拿到 CampaignVec2 实例
CampaignVec2 campaignVec2 = ...;
var result = campaignVec2.GetHashCode();
```

## 使用示例

```csharp
CampaignVec2.AutoGeneratedStaticCollectObjectsCampaignVec2(o, collectedObjects);
```

## 参见

- [本区域目录](../)