# v1.4.5 H3-B Party Entity Deep-Page Wave

> Documentation-only wave. All body prose must be handwritten from the 1.4.5 Bannerlord.Source declarations and real call sites.

## Goal

Replace the v1.4.5 signature shells for `CharacterObject`, `PartyBase`, `Town`, and `Village` with task-oriented bilingual deep pages while preserving their existing routes and connecting them to the already-written Campaign object graph.

## Global Constraints

- Source authority is `R:\Bannerlord\bannerlord-1.4.5\Bannerlord.Source`.
- Write only source-backed prose; do not run or use signature-to-prose, stub, fragment, or bulk body generators.
- Preserve existing URLs and unrelated dirty files. Do not pull, stash, reset, clean, commit, or push.
- Every page must explain the mental model, acquisition path, lifecycle, when/when-not guidance, dependencies, real call sites, mutation boundaries, risks, and reciprocal navigation.
- Examples must use real Bannerlord API paths and must not contain `...`, fake services, or invented type names.
- R1 remains incomplete while the gap is nonzero; this wave must not mark the global goal complete.

## Tasks

### Task 1: CharacterObject

Modify the Chinese and English `v1.4.5/api/campaign/CharacterObject.md` pages. Cover the distinction between a registered character/troop template and `Hero`, global lookup through `Campaign.Current.Characters` and `CharacterObject.Find*`, Hero and roster relationships, upgrade and wage/model consumers, save/load identity, and the Action boundary for party membership, prisoners, killing, or other world changes.

QA: source/call-site review, no boilerplate or placeholders, bilingual quality audit, link audit, and handwritten coverage classification.

### Task 2: PartyBase

Modify the Chinese and English `v1.4.5/api/campaign/PartyBase.md` pages. Cover the shared boundary owned by `Settlement` or `MobileParty`, `PartyBase.MainParty`, roster and owner derivation, MapEvent/encounter/Action consumers, lifecycle and destruction, save/load repair, cache/state risks, and why a mod should use the host and relevant Actions rather than constructing or mutating an orphan PartyBase.

QA: source/call-site review, no boilerplate or placeholders, bilingual quality audit, link audit, and handwritten coverage classification.

### Task 3: Town and Village

Status: complete - bilingual handwritten deep pages reviewed against Town/Village, Settlement, daily-tick, Action, Model, and trade-behavior call sites

Modify the Chinese and English `v1.4.5/api/campaign/Town.md` and `Village.md` pages. Cover Settlement component ownership, Bound/TradeBound, town prosperity/food/security/loyalty/militia/economy, village hearth/production/trade, daily behaviors and model consumers, ownership/governor/building/workshop boundaries, and safe Action-backed mutations.

QA: source/call-site review, no boilerplate or placeholders, bilingual quality audit, link audit, and handwritten coverage classification.

### Task 4: Wave evidence and navigation

Status: complete - Campaign indexes updated; bilingual coverage, quality, navigation, section-tree, page-navigation, body-writer, link, npm test, and R1 check evidence collected; docs build attempted and recorded as BUILD_ENV_BLOCKED on R:; R1 remains incomplete

Refresh only the relevant Campaign index entries and generated evidence. Run bilingual handwritten coverage and R1 reports in check mode, quality, body-writer, navigation, section-tree, page-navigation, link, and `npm test` audits. Attempt `npm run docs:build` and record any R: drive canonical-path environment failure without reclassifying it as a pass.

QA: `PartyBase`, `Town`, `Village`, and `CharacterObject` classify as deep handwritten in both languages; all required gates pass except any explicitly recorded environment blocker; R1 gap remains visible.
