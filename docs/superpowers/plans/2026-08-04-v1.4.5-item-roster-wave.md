# v1.4.5 Item Object-Roster Handwritten Wave Implementation Plan

> **For agentic workers:** Use a source-review author followed by an independent reviewer. This wave is documentation-only and must preserve the dirty checkout.

**Goal:** Replace the v1.4.5 `ItemObject`, `Equipment`, and `ItemRoster` Chinese and English signature shells with a source-backed handbook chain that explains item identity, equipment composition, and safe inventory reads and mutations.

**Architecture:** Keep the existing URLs in `content/v1.4.5/{zh,en}/api/core-extra/ItemObject.md`, `content/v1.4.5/{zh,en}/api/core-extra/Equipment.md`, and `content/v1.4.5/{zh,en}/api/campaign/ItemRoster.md`. The pages are a dependency-ordered chain: `ItemObject` identifies registered content, `Equipment` combines an item with modifiers and slots, and `ItemRoster` stores counted `ItemRosterElement` values in party/settlement-owned or temporary containers. Each language gets the same model and safety boundaries in its own prose.

**Tech Stack:** Markdown, Zola, Bannerlord 1.4.5 decompiled C# source, Node.js documentation audits.

## Global Constraints

- All body prose is handwritten after reading source declarations and real call sites; signatures are evidence only.
- Do not invoke retired body generators, stub writers, signature-to-prose tools, or bulk content mutators.
- Preserve existing URLs and unrelated dirty-worktree changes; do not reset, clean, stash, commit, or push.
- Use v1.4.5 as authority and mention v1.3.15 only when a verified difference matters.
- Each page needs frontmatter, metadata, a one-sentence responsibility, mental model, when/when-not guidance, dependencies, lifecycle/save risks, real acquisition paths, meaningful member guidance, real examples, and reciprocal navigation.
- Generics in prose must be enclosed in backticks; examples may not contain `...`, fake services, invented type names, or direct field writes that bypass the roster contract.
- `npm test`, link audit, quality audit, body-writer audit, navigation, section-tree, page-navigation, and scoped handwritten/R1 checks are evidence gates. A persistent R: drive Zola canonical-path error must be reported as an environment blocker, never as a build pass.

## Success Criteria

- All six target pages classify as `deep_pass` with zero reject-pattern hits.
- The pages connect `ItemObject` identity to `EquipmentElement`, `ItemRosterElement`, and `ItemRoster` without treating generated save helpers as modder-facing prose.
- The pages explain `MobileParty.MainParty.ItemRoster`, `Settlement.CurrentSettlement.ItemRoster`, and a temporary `new ItemRoster(existingRoster)` copy as distinct acquisition paths.
- The pages explain `AddToCounts`, `RemoveIf`, `Clear`, index/copy semantics, `VersionNo`/cached totals, `RosterUpdatedEvent`, `ItemRosterElement.Amount`, and the relevant Action/event boundaries.
- All target links resolve and the full structural gates remain green.
- The evidence ledger records exact before/after coverage and explicitly keeps the global R1/H10 goal incomplete while gaps, curated navigation gaps, stubs, or build blockers remain.

## Tasks

### Task 1: Source and call-site dossier

**Files:**
- Read: `R:\Bannerlord\bannerlord-1.4.5\Bannerlord.Source\bin\TaleWorlds.CampaignSystem\TaleWorlds.CampaignSystem.Roster\ItemRoster.cs`
- Read: `R:\Bannerlord\bannerlord-1.4.5\Bannerlord.Source\bin\TaleWorlds.Core\TaleWorlds.Core\ItemRosterElement.cs`
- Read: `R:\Bannerlord\bannerlord-1.4.5\Bannerlord.Source\bin\TaleWorlds.CampaignSystem\TaleWorlds.CampaignSystem.Actions\GiveItemAction.cs`
- Read: `R:\Bannerlord\bannerlord-1.4.5\Bannerlord.Source\bin\TaleWorlds.CampaignSystem\TaleWorlds.CampaignSystem.Actions\SellItemsAction.cs`
- Read: `R:\Bannerlord\bannerlord-1.4.5\Bannerlord.Source\bin\TaleWorlds.Core\TaleWorlds.Core\ItemObject.cs`
- Read: `R:\Bannerlord\bannerlord-1.4.5\Bannerlord.Source\bin\TaleWorlds.Core\TaleWorlds.Core\Equipment.cs`
- Read: `R:\Bannerlord\bannerlord-1.4.5\Bannerlord.Source\bin\TaleWorlds.Core\TaleWorlds.Core\EquipmentElement.cs`
- Read: relevant `MobileParty`, `PartyBase`, `SettlementComponent`, `InventoryLogic`, food/trade/workshop call sites.

**QA:** Confirm every prose claim in Tasks 2-3 has a source declaration or call-site basis; do not modify repository files in this task.

### Task 2: ItemObject bilingual deep pages

**Files:**
- Modify: `content/v1.4.5/zh/api/core-extra/ItemObject.md`
- Modify: `content/v1.4.5/en/api/core-extra/ItemObject.md`

**Required content:** Replace each generated shell. Cover registered object identity and acquisition through `MBObjectManager`, static/default items versus campaign item definitions, equipment/component relationships, read-only content versus mutable inventory state, save identity risks, real call sites, version notes, and Parent/Sibling/Related navigation.

**QA:** Both pages classify `deep_pass`; no placeholder or formulaic sentence; examples use real object lookup paths; all links resolve.

### Task 3: Equipment bilingual deep pages

**Files:**
- Modify: `content/v1.4.5/zh/api/core-extra/Equipment.md`
- Modify: `content/v1.4.5/en/api/core-extra/Equipment.md`

**Required content:** Replace each generated shell. Explain `Equipment` as a fixed equipment-set value object, slot access and invalid/default values, relation to `EquipmentElement` and `ItemObject`, modifier and culture/weapon consumers, copying/equality boundaries, and why it does not mutate a party inventory by itself.

**QA:** Both pages classify `deep_pass`; examples and risk language match across languages; no placeholder or formulaic sentence; all links resolve.

### Task 4: ItemRoster bilingual deep pages

**Files:**
- Modify: `content/v1.4.5/zh/api/campaign/ItemRoster.md`
- Modify: `content/v1.4.5/en/api/campaign/ItemRoster.md`

**Required content:** Replace the entire generated shell. Cover ownership and lifetime, authoritative versus temporary rosters, `ItemRosterElement` identity and amount, cached-stat invalidation and `VersionNo`, event timing, safe queries and mutations, invalid-index behavior, transfer Actions, save serialization, real acquisition paths, and Parent/Sibling/Related navigation.

**QA:** Both pages classify `deep_pass`; no placeholder or formulaic sentence; no direct roster replacement or fake acquisition example; all links resolve.

### Task 5: Independent review and evidence

**Files:**
- Review: the two target pages and any direct diff they create.
- Update only the current automation progress/evidence ledger if one exists for this wave.
- Refresh generated report outputs only when the canonical commands prove them stale.

**Commands:**
- `npm test`
- `npm run audit:links`
- `node tools/audit-doc-quality.mjs content --scope v1.4.5`
- `npm run audit:body-writers`
- `npm run audit:navigation`
- `npm run audit:section-tree`
- `npm run audit:page-navigation`
- Scoped v1.4.5 Chinese and English handwritten/R1 report generation followed by serial `--check` runs.
- `npm run docs:build` independently; record the known R: mount failure if reproduced.

**QA:** Reviewer returns PASS only after inspecting source claims, placeholder scan, bilingual parity, link targets, navigation blocks, and risk coverage. Report exact counts and preserve the non-completion boundary for R1/H10.
