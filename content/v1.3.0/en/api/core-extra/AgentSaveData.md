---
title: "AgentSaveData"
description: "Auto-generated class reference for AgentSaveData."
---
# AgentSaveData

**Namespace:** TaleWorlds.Core
**Module:** TaleWorlds.Core
**Type:** `public struct AgentSaveData`
**Base:** none
**File:** `TaleWorlds.Core/AgentSaveData.cs`

## Overview

`AgentSaveData` behaves like a data carrier: it packages fields so systems can exchange state in a structured form.

## Mental Model

Treat `AgentSaveData` as a Data-style extension point: first identify who creates it, who owns it, and who calls it, then decide whether you should subclass it, compose it, or only read from it.

## Key Methods

### UpdateSpawnFrame
`public void UpdateSpawnFrame(MatrixFrame spawnFrame)`

**Purpose:** Recalculates and stores the latest representation of `spawn frame`.

```csharp
// Obtain an instance of AgentSaveData from the subsystem API first
AgentSaveData agentSaveData = ...;
agentSaveData.UpdateSpawnFrame(spawnFrame);
```

### AutoGeneratedStaticCollectObjectsAgentSaveData
`public static void AutoGeneratedStaticCollectObjectsAgentSaveData(object o, List<object> collectedObjects)`

**Purpose:** Performs the operation described by this method.

```csharp
// Static call; no instance required
AgentSaveData.AutoGeneratedStaticCollectObjectsAgentSaveData(o, collectedObjects);
```

## Usage Example

```csharp
// This data object is usually returned by campaign/mission APIs
AgentSaveData entry = ...;
```

## See Also

- [Area Index](../)