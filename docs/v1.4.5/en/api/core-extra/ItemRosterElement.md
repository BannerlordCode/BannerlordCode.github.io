<!-- BEGIN BREADCRUMB -->
**Home** → **API Index** → **Area** → `ItemRosterElement`
- [← Area / Back to core-extra](./)
- [↑ API Index](../)
- [🏠 Home v1.4.5](../../)
- [⭐ Version Architecture](../../architecture/)
<!-- END BREADCRUMB -->
# ItemRosterElement

**Namespace:** TaleWorlds.Core
**Module:** TaleWorlds.Core
**Type:** `public struct ItemRosterElement : ISerializableObject, ISavedStruct`
**Base:** `ISerializableObject`
**File:** `Bannerlord.Source/bin/TaleWorlds.Core/TaleWorlds.Core/ItemRosterElement.cs`

## Overview

`ItemRosterElement` lives in `TaleWorlds.Core` and exposes the state, behavior, or workflow entry points of that subsystem to mod developers through its public members. Read its properties as “what state it owns” and its methods as “what actions it allows”.

## Mental Model

Start from namespace `TaleWorlds.Core` to place it in the stack, then inspect its public methods: if it mainly exposes Get/Set members, it is likely a state object; if it centers on Create/Apply/Execute verbs, it behaves more like a service or workflow entry point.

## Key Properties

| Name | Signature |
|------|-----------|
| `EquipmentElement` | `public EquipmentElement EquipmentElement { get; }` |
| `Amount` | `public int Amount { get; set; }` |

## Key Methods

### AutoGeneratedStaticCollectObjectsItemRosterElement
`public static void AutoGeneratedStaticCollectObjectsItemRosterElement(object o, List<object> collectedObjects)`

**Purpose:** Handles logic related to `auto generated static collect objects item roster element`.

### GetHashCode
`public override int GetHashCode()`

**Purpose:** Gets the current value of `hash code`.

### ToString
`public override string ToString()`

**Purpose:** Handles logic related to `to string`.

### Clear
`public void Clear()`

**Purpose:** Handles logic related to `clear`.

### IsEqualTo
`public bool IsEqualTo(ItemRosterElement other)`

**Purpose:** Handles logic related to `is equal to`.

### Equals
`public override bool Equals(object obj)`

**Purpose:** Handles logic related to `equals`.

### GetRosterElementWeight
`public float GetRosterElementWeight()`

**Purpose:** Gets the current value of `roster element weight`.

## Usage Example

```csharp
ItemRosterElement.AutoGeneratedStaticCollectObjectsItemRosterElement(o, collectedObjects);
```

## See Also

- [Complete Class Catalog](../catalog)