---
title: Program (GauntletUI CodeGenerator)
description: Program - Gauntlet UI 代码生成器入口，为各模块?Prefab 生成数据绑定 C# 代码
---
<!-- BEGIN BREADCRUMB -->
**首页** → **API 目录** → **本领域** → `Program`
- [← 本领域 / 返回 campaign-ext](./)
- [↑ API 目录](../)
- [🏠 首页 v1.3.15](../../)
- [⭐ SDK 总览](../../architecture/sdk-overview)
<!-- END BREADCRUMB -->
# Program

**命名空间:** TaleWorlds.MountAndBlade.GauntletUI.CodeGenerator
**模块:** TaleWorlds.MountAndBlade.GauntletUI.CodeGenerator
**类型:** internal class
**领域:** UI 工具?/ 代码生成

## 概述

`Program

` ?Bannerlord Gauntlet UI 代码生成器的命令行入口。它是一个独立的可执行程序（非游戏运行时组件），在构建期扫描各模块的 GUI Prefab 目录，为每个 Prefab 生成对应?C# 数据绑定代码（`AutoGenerated

` 系列）。生成的代码包含 Prefab ?widget 树结构、数据绑定路径、事件连接等，让 ViewModel 可以通过强类型属性访?UI 元素?
`Main

` 方法依次调用 5 个生成函数：

`GenerateBannerlord

`、`GenerateMultiplayer

`、`GenerateSandBox

`、`GenerateStoryMode

`、`GenerateNavalDLC

`。每个函数负责一个模块的 Prefab 生成，流程一致：1) 指定 GUI 目录路径数组?) 创建 

`UICodeGenerationContext

`（指定输出命名空间与目录）；3) 

`Prepare

` 扫描目录?) 

`AddVariant

` 注册每个 Prefab 与其 DataSource 类型?) 

`Generate

` 输出 C# 文件?
`AddVariant

` 是关键辅助方法：它把 Prefab 名与 DataSource ViewModel 类型注册?

`UICodeGenerationContext

`，用 

`UICodeGenerationDatabindingVariantExtension

` 扩展。多?Context 之间?

`index++ % length

` 轮询分配，平衡生成负载。生成的代码被各模块引用，运行时?Gauntlet 引擎加载 Prefab 并实例化对应?AutoGenerated 类型?
## 心智模型

?

`Program

` 看作"UI Prefab ?C# 代码的编译器"。工作流?
1. 每个 

`UICodeGenerationContext

` 对应一个输出程序集（如 

`TaleWorlds.MountAndBlade.GauntletUI.AutoGenerated0

`）?2. 

`Prepare

` 扫描 GUI 目录，解析所?

`.prefab

` 文件，建?widget 树与绑定路径映射?3. 

`AddVariant

` ?Prefab 名与 DataSource 类型配对——代码生成器据此生成强类型访问代码（?

`this._hintTextWidget = this.FindChild("HintText") as TextWidget

`）?4. 

`Generate

` 输出 

`.cs

` 文件到指定目录，这些文件编译后成?AutoGenerated 程序集?
`AddVariant

` 的轮询（

`index++ % generationContexts.Length

`）把 Prefab 分散到多?Context（如 Bannerlord ?2 ?Context），避免单个程序集过大?
Mod 开发者不直接使用此类——它?TaleWorlds 构建管线的一部分。但理解它有助于诊断"Prefab 改了?UI 没变"（需重新生成）?自定?Prefab 的数据绑定不生效"（DataSource 类型未注册）等问题?
## 主要属?
\| 属?\| 类型 \| 说明 \|
\|------\|------\|------\|
\| 

`index

` \| 

`int

` \| 静态计数器，用?

`AddVariant

` 在多?Context 间轮询分?Prefab?\|

## 主要方法

### Main
`

`

`csharp
[STAThread]
private static int Main(string[] args)
`

`

`
程序入口。打印引用程序集名（

`SceneWidget

`/

`TooltipWidget

`/

`PortScreenWidget

` 触发程序集加载），设置不变文化，依次调用 5 个生成函数?
### GenerateBannerlord
`

`

`csharp
private static void GenerateBannerlord()
`

`

`
生成核心 Bannerlord UI。扫?

`WOTS/GUI/GauntletUI/

` ?

`WOTS/Modules/Native/GUI/

`，输出到 2 ?Context（`AutoGenerated0

`/

`AutoGenerated1

`）。注册约 20 ?Prefab 变体：聊天、面容生成、命令轮盘、初始菜单?credits、逃逸菜单、击杀提示、HUD、通知、工具提示、弹窗、十字准星、装备丢弃、计分板等?
### GenerateMultiplayer
`

`

`csharp
private static void GenerateMultiplayer()
`

`

`
生成多人模式 UI。扫?Bannerlord + Native + Multiplayer GUI 目录，输出到 1 ?Context。注册约 18 ?Prefab 变体：决斗、计分板、任务标记、聊天、击杀提示、死亡卡片、投票、回合结束、服务器状态、逃逸菜单、举报、管理员信息、队?文化选择、休息大厅、HUD 扩展等?
### GenerateSandBox
`

`

`csharp
private static void GenerateSandBox()
`

`

`
生成沙盒模式 UI（最大批量）。扫?Bannerlord + Native + SandBoxCore + SandBox GUI 目录，输出到 2 ?Context。注册约 60 ?Prefab 变体：库存、地图栏、氏族管理、队伍名牌、对话、百科、锦标赛、名称标记、招募、围城、通知、追踪、存档、城镇管理、交易、军团管理、旗帜编辑器、游戏菜单、锻造、角色开发、角色创建、任务、战役选项、计分板、百科页面等?
### GenerateStoryMode
`

`

`csharp
private static void GenerateStoryMode()
`

`

`
生成故事模式 UI。扫?Bannerlord + Native + SandBoxCore + SandBox + StoryMode GUI 目录，输出到 1 ?Context。注?1 ?Prefab 变体：训练场目标（`TrainingFieldObjectives

`）?
### GenerateNavalDLC
`

`

`csharp
private static void GenerateNavalDLC()
`

`

`
生成海军 DLC UI。扫?Bannerlord + Native + SandBoxCore + SandBox + StoryMode + NavalDLC GUI 目录，输出到 1 ?Context。注?1 ?Prefab 变体：港口界面（

`PortScreen

`）?
### AddVariant
`

`

`csharp
private static void AddVariant(UICodeGenerationContext[] generationContexts, Type datasourceType, string prefabName)
`

`

`
注册 Prefab 变体。创?

`DataSourceType

` 字典，用 

`index++ % length

` 轮询选择 Context，调?

`AddPrefabVariant

` 注册 Prefab 名、DataSource 全名、数据绑定扩展?
## 使用示例

### 示例: 理解自定?Prefab 如何进入代码生成

**场景**: Mod 添加了自定义 Prefab 

`MyModScreen

`，需要生成数据绑定代码以?ViewModel 强类型访?UI 元素?
`

`

`csharp
// Mod 的代码生成器（若 Mod 需要自己的 AutoGenerated 代码?// 通常 Mod 不需要自己写代码生成器——Gauntlet 运行时会动态加?Prefab
// 但若需要强类型访问，可参?Program 的模式：

public static void GenerateMyMod()
{
    var context = new UICodeGenerationContext(
        "MyMod.GauntletUI.AutoGenerated",
        "MyMod\\Generated");

    context.Prepare(
        new string[] { "MyMod/GUI/" },
        new PrefabExtension[] { new PrefabDatabindingExtension() });

    // 注册 Prefab ?DataSource 类型
    var dict = new Dictionary&lt;string, object&gt;();
    dict.Add("DataSourceType", typeof(MyModScreenVM));
    context.AddPrefabVariant(
        "MyModScreen",
        typeof(MyModScreenVM).FullName,
        new UICodeGenerationDatabindingVariantExtension(),
        dict);

    context.Generate();
}

// 但大多数 Mod 用更简单的方式：运行时动态加载，无强类型
// var movie = this._gauntletUIEngine.CreateMovie("MyModScreen", viewModel);
// 这种方式不需要代码生成，但访?UI 元素需通过 ViewModel 绑定
`

`

`

**要点**: 

`Program

` 是构建期工具，Mod 通常不直接使用；运行时动态加?Prefab 不需?AutoGenerated 代码；`AddVariant

` ?DataSource 类型必须?ViewModel 匹配，否则绑定失败；GUI 目录路径是相对构建工作目录的（`../WOTS/...

`），Mod 需调整路径；生成代码编译后成为模块程序集的一部分，运行时?

`GauntletMovie

` 加载?
## 参见

- [完整类目录](../catalog)
- [本领域目录](../catalog-campaign)
- [API 目录](../)
- [SDK 总览](../../architecture/sdk-overview)
