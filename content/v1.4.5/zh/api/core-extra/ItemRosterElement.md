---
title: "ItemRosterElement"
description: "ItemRosterElement 的自动生成类参考。"
---
# ItemRosterElement

**Namespace:** TaleWorlds.Core
**Module:** TaleWorlds.Core
**Type:** `public struct ItemRosterElement : ISerializableObject, ISavedStruct`
**Base:** `ISerializableObject`
**File:** `bin/TaleWorlds.Core/TaleWorlds.Core/ItemRosterElement.cs`

## 概述

`ItemRosterElement` 位于 `TaleWorlds.Core`，它通过这组公开成员把对应子系统的状态、行为或流程入口暴露给 mod 开发者。阅读时先看属性代表“它持有什么状态”，再看方法代表“它允许你做什么”。

## 心智模型

先从命名空间 `TaleWorlds.Core` 判断它属于哪层系统，再看公开方法：如果以 Get/Set 为主，它多半是状态对象；如果以 Create/Apply/Execute 为主，它更像服务或流程入口。

## 主要属性

| Name | Signature |
|------|-----------|
| `EquipmentElement` | `public EquipmentElement EquipmentElement { get; }` |
| `Amount` | `public int Amount { get; set; }` |

## 主要方法

### AutoGeneratedStaticCollectObjectsItemRosterElement
`public static void AutoGeneratedStaticCollectObjectsItemRosterElement(object o, List<object> collectedObjects)`

**用途 / Purpose:** 处理与 「auto generated static collect objects item roster element」 相关的逻辑。

```csharp
// 静态调用，不需要实例
ItemRosterElement.AutoGeneratedStaticCollectObjectsItemRosterElement(o, collectedObjects);
```

### GetHashCode
`public override int GetHashCode()`

**用途 / Purpose:** 返回当前对象的哈希码，用于字典或哈希集合中的快速查找。

```csharp
// 先通过子系统 API 拿到 ItemRosterElement 实例
ItemRosterElement itemRosterElement = ...;
var result = itemRosterElement.GetHashCode();
```

### ToString
`public override string ToString()`

**用途 / Purpose:** 返回当前对象的人类可读字符串表示。

```csharp
// 先通过子系统 API 拿到 ItemRosterElement 实例
ItemRosterElement itemRosterElement = ...;
var result = itemRosterElement.ToString();
```

### Clear
`public void Clear()`

**用途 / Purpose:** 清空当前对象中的内容。

```csharp
// 先通过子系统 API 拿到 ItemRosterElement 实例
ItemRosterElement itemRosterElement = ...;
itemRosterElement.Clear();
```

### IsEqualTo
`public bool IsEqualTo(ItemRosterElement other)`

**用途 / Purpose:** 判断当前对象是否处于 「equal to」 状态或条件。

```csharp
// 先通过子系统 API 拿到 ItemRosterElement 实例
ItemRosterElement itemRosterElement = ...;
var result = itemRosterElement.IsEqualTo(other);
```

### Equals
`public override bool Equals(object obj)`

**用途 / Purpose:** 比较当前对象与传入实例是否相等。

```csharp
// 先通过子系统 API 拿到 ItemRosterElement 实例
ItemRosterElement itemRosterElement = ...;
var result = itemRosterElement.Equals(obj);
```

### GetRosterElementWeight
`public float GetRosterElementWeight()`

**用途 / Purpose:** 读取并返回当前对象中 「roster element weight」 的结果。

```csharp
// 先通过子系统 API 拿到 ItemRosterElement 实例
ItemRosterElement itemRosterElement = ...;
var result = itemRosterElement.GetRosterElementWeight();
```

## 使用示例

```csharp
ItemRosterElement.AutoGeneratedStaticCollectObjectsItemRosterElement(o, collectedObjects);
```

## 参见

- [本区域目录](../)