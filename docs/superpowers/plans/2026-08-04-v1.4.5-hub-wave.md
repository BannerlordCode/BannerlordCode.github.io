# v1.4.5 Handwritten Hub Wave Implementation Plan

> **For agentic workers:** This is a documentation-only wave. Read the matching v1.4.5 doc contract before editing. All body prose is handwritten from source and call sites; do not use body generators.

**Goal:** Turn the v1.4.5 `campaign-ext` API forest into a task-oriented bilingual entry point for Foundation, Campaign, Mission, Action, Model, and Save workflows without mass-writing long-tail stubs.

**Architecture:** Keep every existing v1.4.5 URL and alphabetical catalog, but replace the `campaign-ext` hub pair with handwritten mental models, dependency maps, safe reading orders, and task links into existing Campaign, Mission, Foundation, Action, Model, and Save pages. Do not add new section routes in this wave because the shared worktree already contains concurrent navigation changes. The hub pages are structural navigation content; they do not pretend that untouched leaf pages are complete.

**Tech Stack:** Markdown, Zola section routes, `data/section-tree.json`, `data/page-navigation.json`, Node.js audit scripts.

## Global Constraints

- All product prose under `content/**` is manually written after reading authoritative `bannerlord-1.4.5` source and relevant `bannerlord-1.3.15` call sites.
- Do not run or use `generate-class-docs`, `gen-class-ref`, `batch-gen-stubs`, `doc-fragments`, `enhance-stubs`, `populate-curated-content`, or any signature-to-prose writer.
- Preserve existing leaf URLs and the existing alphabetical catalogs.
- Every hub has informative frontmatter, a mental model, dependency and risk boundaries, real API examples or acquisition paths, Parent/Sibling/Child navigation, and a visible statement of remaining leaf-page coverage.
- Every new link must resolve under the repository's clean-route rules; `BROKEN_LINKS=0` is mandatory.
- Keep Chinese and English semantically parallel; use v1.4.5 source as authority and note v1.3.15 differences only when relevant.
- Do not commit or reset the shared dirty worktree.

---

### Task 1: Campaign-Ext task hub

**Files:**
- Modify: `content/v1.4.5/zh/api/campaign-ext/_index.md`
- Modify: `content/v1.4.5/en/api/campaign-ext/_index.md`

**Content contract:** Replace the directory-only framing with a task-first map covering module loading, Campaign behavior registration, safe world-state Actions, Model selection, Mission behavior, custom save data, and Foundation/Core lookup. Keep the complete existing alphabetical child catalog after the new sections and explicitly label it as identity lookup rather than semantic coverage.

**Source evidence:** `CampaignGameStarter.cs`, `CampaignBehaviorBase.cs`, `CampaignBehaviorManager.cs`, `CampaignEvents.cs`, `CampaignEventReceiver.cs`, `SandBoxManager.cs`, `TournamentCampaignBehavior.cs`, `MBSubModuleBase.cs`, `Mission.cs`, and `SaveManager.cs` from `bannerlord-1.4.5/Bannerlord.Source`; use matching `bannerlord-1.3.15` files only for version notes.

**Required task routes:**
- SubModule and Behavior registration
- Hero money/kills/kingdom through `*Action.Apply`
- custom save fields through `SyncData(IDataStore)`
- MissionBehavior and Agent removal lifecycle
- Model versus Action for war-score/economic calculation

**QA:** Check both pages for real `MBSubModuleBase`, `Campaign.Current`, `Mission.Current`, `CampaignBehaviorBase`, `CampaignEvents`, `SaveManager`, and `GameModels` paths; run the link audit against all new links. Verify no v1.3.15-only path is presented as v1.4.5 API.

### Task 2: Gates and evidence

**Files:**
- Do not modify: `data/navigation.json`, `data/section-tree.json`, `data/page-navigation.json`, or `templates/macros/sidebar.html` unless a gate proves the two existing hub routes require a minimal repair.
- Create or update: `.superpowers/sdd/2026-08-04-v1.4.5-hub-wave/progress.md`

**Steps:**
- Refresh v1.4.5 zh/en handwritten coverage and R1 reports into their existing report paths, then inspect that hub pages are not miscounted as leaf deep coverage.
- Run `npm test`, `npm run audit:quality`, `npm run audit:body-writers`, `npm run audit:navigation`, `npm run audit:section-tree`, `npm run audit:page-navigation`, and `npm run audit:links`.
- Run `zola build` independently. If the RamDisk canonicalization failure persists, reproduce it and record the exact failure; do not infer build success from Node gates.

**Success criteria:** All structural/content gates pass, `BROKEN_LINKS=0`, both hub pages have Parent/Sibling/Child/Related navigation, no new quality blockers or body-writer violations appear, and the evidence ledger records exact coverage counts plus the actual build result. R1 remains incomplete while the gap is nonzero.
