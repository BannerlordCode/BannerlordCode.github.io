---
title: "D3D11"
description: "Auto-generated class reference for D3D11."
---
# D3D11

**Namespace:** TaleWorlds.TwoDimension.Standalone.Native.Windows
**Module:** TaleWorlds.TwoDimension
**Type:** `public static class D3D11`
**Base:** none
**File:** `bin/TaleWorlds.TwoDimension.Standalone/TaleWorlds.TwoDimension.Standalone.Native.Windows/D3D11.cs`

## Overview

`D3D11` lives in `TaleWorlds.TwoDimension.Standalone.Native.Windows` and exposes the state, behavior, or workflow entry points of that subsystem to mod developers through its public members. Read its properties as “what state it owns” and its methods as “what actions it allows”.

## Mental Model

Start from namespace `TaleWorlds.TwoDimension.Standalone.Native.Windows` to place it in the stack, then inspect its public methods: if it mainly exposes Get/Set members, it is likely a state object; if it centers on Create/Apply/Execute verbs, it behaves more like a service or workflow entry point.

## Key Methods

### D3D11CreateDevice
`public static extern int D3D11CreateDevice(DXGI.IDXGIAdapter adapter, D3D_DRIVER_TYPE driverType, IntPtr software, uint flags, IntPtr featureLevels, int featureLevelCount, int sdkVersion, out IntPtr ppDevice, IntPtr pFeatureLevel, out IntPtr ppImmediateContext)`

**Purpose:** Performs the operation described by this method.

```csharp
// Static call; no instance required
D3D11.D3D11CreateDevice(adapter, driverType, software, 0, featureLevels, 0, 0, ppDevice, pFeatureLevel, ppImmediateContext);
```

## Usage Example

```csharp
D3D11.D3D11CreateDevice(adapter, driverType, software, 0, featureLevels, 0, 0, ppDevice, pFeatureLevel, ppImmediateContext);
```

## See Also

- [Area Index](../)