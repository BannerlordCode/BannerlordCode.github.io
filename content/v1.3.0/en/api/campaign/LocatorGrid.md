---
title: "LocatorGrid"
description: "Auto-generated class reference for LocatorGrid."
---
# LocatorGrid

**Namespace:** TaleWorlds.CampaignSystem.Map
**Module:** TaleWorlds.CampaignSystem
**Type:** `internal class LocatorGrid<T>`
**Base:** none
**File:** `TaleWorlds.CampaignSystem/Map/LocatorGrid.cs`

## Overview

`LocatorGrid` lives in `TaleWorlds.CampaignSystem.Map` and exposes the state, behavior, or workflow entry points of that subsystem to mod developers through its public members. Read its properties as “what state it owns” and its methods as “what actions it allows”.

## Mental Model

Start from namespace `TaleWorlds.CampaignSystem.Map` to place it in the stack, then inspect its public methods: if it mainly exposes Get/Set members, it is likely a state object; if it centers on Create/Apply/Execute verbs, it behaves more like a service or workflow entry point.

## Usage Example

```csharp
// Obtain an instance from the relevant subsystem API
LocatorGrid instance = ...;
```

## See Also

- [Area Index](../)