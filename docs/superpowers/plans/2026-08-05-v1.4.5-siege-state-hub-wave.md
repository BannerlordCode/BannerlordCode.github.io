# Bannerlord v1.4.5 Siege Repair and State Hub Wave

> **For agentic workers:** This plan is executed in the existing shared checkout. Do not create a worktree, commit, push, reset, clean, stash, or use any body generator.

**Goal:** Repair the currently edited Siege documentation and URL regressions, then replace the next four state-bridge API stubs with source-backed bilingual pages.

**Architecture:** Keep the existing v1.4.5 routes and section layout. The Siege repair task owns only the currently failing Siege/link surfaces. The state-hub task owns exactly eight pages: `GameState`, `GameStateManager`, `MissionGameModels`, and `EncounterGameMenuModel` in English and Chinese. Each page is written from `bannerlord-1.4.5/Bannerlord.Source` and call sites, with reciprocal navigation and explicit lifecycle/risk boundaries.

**Tech Stack:** Zola Markdown, Node.js audit scripts, C# source under `bannerlord-1.4.5/Bannerlord.Source`.

## Global Constraints

- Product prose is handwritten and source-backed; signatures are evidence, never the body.
- Every page has frontmatter, responsibility, mental model, use/do-not-use guidance, dependencies, risks, real acquisition path, member timing, and Parent/Sibling/Related navigation.
- Use concrete APIs and source-confirmed call sites; no `SomeValue`, `service = ...`, ellipses, invented type names, or side-effectful examples presented as reads.
- Preserve existing URLs and unrelated dirty changes. Do not run `generate-class-docs.mjs`, `gen-class-ref.mjs`, `batch-gen-stubs.mjs`, `enhance-stubs.mjs`, `doc-fragments.mjs`, `populate-curated-content.mjs`, or other body generators.
- Run verification serially on one observed snapshot. `BROKEN_LINKS=0`, `npm test` 64/64, quality `Blockers=0`, and explicit report `--check` are required for a bounded-wave pass; nonzero global R1 gap and mapped-`R:` Zola build failure must be reported honestly.

### Task 1: Repair current links and Siege review findings

**Files:**
- Modify only the 12 files named by `node tools/audit-links.mjs` under `content/v1.4.5/{en,zh}/api/{campaign,campaign-ext}`.
- Modify the current Siege pages `BlockadeBattleMapEvent.md`, `SiegeEventManager.md`, and `SiegeAmbushEventComponent.md` in both languages, plus the v1.4.5 campaign `_index.md` in both languages.

**Required changes:**
- Resolve every listed URL link using route-relative paths that exist in the current tree.
- Explain `BlockadeBattleMapEvent` through the source-confirmed `CreateBlockadeBattleMapEvent` path; do not recommend `StartBlockadeBattleMapEvent` as the component factory.
- Make the `SiegeEventManager` example observational and side-effect free; it must not call `StartSiegePreparation` or another state-changing entry point.
- Add a task-first Campaign entry that makes the Siege management cluster reachable, while preserving reciprocal links.
- Use a classifier-recognized Chinese responsibility heading on `SiegeAmbushEventComponent` without changing its meaning.

**QA:** Run `node tools/audit-links.mjs`, `npm test`, `node tools/audit-doc-quality.mjs content`, and inspect the changed pages for exact source-backed links and examples.

### Task 2: Write the state-bridge pages

**Files:**
- Replace `content/v1.4.5/{en,zh}/api/core-extra/GameState.md`.
- Replace `content/v1.4.5/{en,zh}/api/core-extra/GameStateManager.md`.
- Replace `content/v1.4.5/{en,zh}/api/mission-ext/MissionGameModels.md`.
- Replace `content/v1.4.5/{en,zh}/api/campaign/EncounterGameMenuModel.md`.

**Source authority and required coverage:**
- `TaleWorlds.Core/GameState.cs` and `GameStateManager.cs`: creation, activation, deactivation, termination, listeners, stack levels, `CreateState<T>`, `PushState`, `PopState`, `CleanStates`, and the `MissionState` acquisition path through `Game.Current.GameStateManager`.
- `TaleWorlds.MountAndBlade/MissionGameModels.cs`: mission-scoped model binding, `MissionGameModels.Current`, construction/cleanup, model consumers, and why it is not `Campaign.Current.Models`.
- `TaleWorlds.CampaignSystem.ComponentInterfaces/EncounterGameMenuModel.cs`: `Campaign.Current.Models.EncounterGameMenuModel`, default implementation, menu-id and start/join battle decisions, and the `EncounterGameMenuBehavior`/town/siege call chain. Explain that it calculates menu results and does not mutate encounter state.

**QA:** For each language, run handwritten coverage and R1 coverage with explicit `.tmp` output and `--check`; then run the full link, quality, body-writer, navigation, section-tree, page-navigation, inventory, and test gates. Confirm all eight pages classify `deep_pass`, contain real examples, and have reciprocal navigation.

## Final Evidence Contract

Report the exact current HEAD/status snapshot, changed paths, command exit codes and key counts, bounded handwritten/R1 numerator and gap for both languages, manual scenario results, and build status. Never call the global R1/H10 objective complete while the coverage gap is nonzero.
