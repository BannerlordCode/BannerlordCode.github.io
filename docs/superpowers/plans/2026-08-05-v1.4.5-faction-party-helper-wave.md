# Bannerlord v1.4.5 Faction and Party Helper Wave

## Goal

Replace the generated `FactionHelper` pages in both languages and repair the existing handwritten `PartyBaseHelper` examples so the two related helper identities satisfy the handwritten-page contract without changing navigation artifacts.

## Source contract

- `FactionHelper` mixes read-only faction queries, UI validation callbacks, campaign eligibility gates, garrison calculations, and a small set of mutation-bearing hostile-action methods.
- `IsClanNameApplicable` and `IsKingdomNameApplicable` return validation tuples; they do not rename the faction.
- `CanPlayerOfferMercenaryService`, `CanPlayerOfferVassalage`, and the `IsMainClanMemberAvailableFor*` methods are gates with `out TextObject` explanations; the caller still owns the Action or UI transition.
- `FinishAllRelatedHostileActions*` and `AdjustFactionStancesForClanJoiningKingdom` can change map-event and party state, dispatch Actions, or reset stance statistics.
- `GetAllyMinorFactions` throws `NotImplementedException` in the v1.4.5 source and must not be presented as a working query.

## Work items

- [x] Audit `FactionHelper.cs`, `PartyBaseHelper.cs`, and real call sites.
- [x] Add fenced real C# examples to the existing `PartyBaseHelper` pages.
- [x] Replace the English and Chinese `FactionHelper` shells by hand.
- [x] Run target classification, banned-pattern, link, quality, coverage, and repository gates.
- [x] Record bounded evidence and preserve the global R1/H10 boundary.

## Scope boundary

Only the four target pages and this plan/ledger belong to the wave. Do not rewrite `SettlementHelper`, modify navigation, generate prose, or claim global R1/H10 completion.
