---
title: DefinitionContext
description: 存档系统的类型定义注册中心，管理所有可存档类型?TypeDefinition/StructDefinition/InterfaceDefinition 等定?
---
<!-- BEGIN BREADCRUMB -->
**首页** → **API 目录** → **本领域** → `DefinitionContext`
- [← 本领域 / 返回 save-system](./)
- [↑ API 目录](../)
- [🏠 首页 v1.3.15](../../)
- [⭐ SDK 总览](../../architecture/sdk-overview)
<!-- END BREADCRUMB -->
# DefinitionContext

**命名空间:** TaleWorlds.SaveSystem.Definition
**模块:** TaleWorlds.SaveSystem
**类型:** ?**领域:** 存档系统 SaveSystem

## 概述

`DefinitionContext

` ?Bannerlord 存档系统?*类型定义注册中心**。它维护了所有可存档类型的元数据定义（`TypeDefinition

`、`StructDefinition

`、`InterfaceDefinition

`、`EnumDefinition

`、`ContainerDefinition

`、`GenericTypeDefinition

`、`BasicTypeDefinition

`），?

`Type

` ?

`SaveId

` 双重索引存储?
在游戏加载或存档时，存档系统通过 

`DefinitionContext

` 查找某个类型对应的定义，从而知道该类型有哪些字段需要序列化、如何构造实例、如何处理版本冲突。`DefinitionContext

` 还管理冲突解决器（`IConflictResolver

`），用于处理跨版本存档的字段迁移?
## 心智模型

?

`DefinitionContext

` 理解为存档系统的"类型数据??
1. **初始化阶?*（`FillWithCurrentTypes

`）：扫描所有引用了 

`TaleWorlds.SaveSystem

` 的程序集，找到所?

`SaveableTypeDefiner

` 子类，按固定顺序（基础类型 ???结构??接口 ?枚举 ?根类 ?泛型 ?容器 ?冲突解决器）注册类型定义
2. **查询阶段**（`TryGetTypeDefinition

` / 

`GetClassDefinition

` 等）：存档系统在序列?反序列化时通过 

`Type

` ?

`SaveId

` 查找类型定义
3. **泛型处理**：遇到未注册的泛型类型时，通过 

`ConstructGenericClassDefinition

` / 

`ConstructGenericStructDefinition

` 动态构造并缓存
4. **冲突解决**：跨版本加载旧存档时，通过 

`IConflictResolver

` 将旧字段映射到新类型

## 主要属?
\| Name \| Signature \| 说明 \|
\|------\|-----------\|------\|
\| 

`GotError

` \| 

`public bool { get; }

` \| 是否有收集类型定义时产生的错?\|
\| 

`Errors

` \| 

`public IEnumerable&lt;string&gt; { get; }

` \| 所有错误消息的只读列表 \|

## 主要方法

### FillWithCurrentTypes

`

`

`csharp
public void FillWithCurrentTypes()
`

`

`

初始化所有类型定义。这?

`DefinitionContext

` 的核心入口方法，执行以下步骤?
1. 通过 

`GetSaveableAssemblies

` 获取所有引用了 

`TaleWorlds.SaveSystem

` 的程序集
2. 从每个程序集中收?

`SaveableTypeDefiner

` 子类实例
3. 按固定顺序调用每?TypeDefiner 的定义方法：

`Initialize

` ?

`DefineBasicTypes

` ?

`DefineClassTypes

` ?

`DefineStructTypes

` ?

`DefineInterfaceTypes

` ?

`DefineEnumTypes

` ?

`DefineRootClassTypes

` ?

`DefineGenericStructDefinitions

` ?

`DefineGenericClassDefinitions

` ?

`DefineContainerDefinitions

` ?

`DefineConflictResolvers

`
4. 为根类和类定义收集初始化回调、属性、字段（类定义使?

`TWParallel.ForEach

` 并行处理?5. 为结构体定义收集属性和字段
6. 汇总所有定义的错误?

`_errors

` 列表
7. 查找并初始化所?

`IAutoGeneratedSaveManager

`

### TryGetTypeDefinition

`

`

`csharp
public TypeDefinitionBase TryGetTypeDefinition(SaveId saveId)
`

`

`

通过 

`SaveId

` 查找类型定义。处理逻辑?
1. 若启用了冲突解决且该 

`SaveId

` 有对应的 

`IConflictResolver

`，返回新类型的定?2. ?

`_allTypeDefinitionsWithId

` 字典中查?3. ?

`SaveId

` ?

`GenericSaveId

`，递归解析基类和泛型参数，通过 

`ConstructTypeFrom

` 构造具体类型，创建 

`GenericTypeDefinition

` 并缓?
### GetTypeDefinition

`

`

`csharp
internal TypeDefinitionBase GetTypeDefinition(Type type)
`

`

`

通过 

`Type

` ?

`_allTypeDefinitions

` 字典中查找类型定义。找不到返回 

`null

`?
### GetClassDefinition

`

`

`csharp
internal TypeDefinition GetClassDefinition(Type type)
`

`

`

专门查找类定义。查找顺序：根类定义 ?泛型类定??普通类定义。若类型是容器则返回 

`null

`?
### GetStructDefinition

`

`

`csharp
internal TypeDefinition GetStructDefinition(Type type)
`

`

`

查找结构体定义。查找顺序：泛型结构体定??普通结构体定义?
### GetInterfaceDefinition / GetEnumDefinition / GetContainerDefinition / GetBasicTypeDefinition

`

`

`csharp
internal InterfaceDefinition GetInterfaceDefinition(Type type)
internal EnumDefinition GetEnumDefinition(Type type)
internal ContainerDefinition GetContainerDefinition(Type type)
internal BasicTypeDefinition GetBasicTypeDefinition(Type type)
`

`

`

分别从对应的字典中按 

`Type

` 查找接口、枚举、容器、基础类型定义?
### HasDefinition

`

`

`csharp
internal bool HasDefinition(Type type)
`

`

`

检?

`_allTypeDefinitions

` 中是否包含指定类型的定义?
### ConstructGenericClassDefinition

`

`

`csharp
internal GenericTypeDefinition ConstructGenericClassDefinition(Type type)
`

`

`

为未注册的泛型类动态构?

`GenericTypeDefinition

`。从泛型基类定义获取 

`TypeSaveId

`，收集泛型参数的 

`SaveId

`，创?

`GenericSaveId

`，复制自定义字段，收集回?字段/属性，并注册到 

`_genericClassDefinitions

`?
### ConstructGenericStructDefinition

`

`

`csharp
internal GenericTypeDefinition ConstructGenericStructDefinition(Type type)
`

`

`

?

`ConstructGenericClassDefinition

` 类似，但用于泛型结构体?
### ConstructContainerDefinition

`

`

`csharp
internal ContainerDefinition ConstructContainerDefinition(Type type, Assembly definedAssembly)
`

`

`

为容器类型（List、CustomList、CustomReadOnlyList、Queue、Dictionary、Array）构?

`ContainerDefinition

`。对?List 类型，额外注册对应的 

`MBList&lt;T&gt;

` ?

`MBReadOnlyList&lt;T&gt;

` 容器定义?
### GetConflictedFieldMemberTypeId / GetConflictedPropertyMemberTypeId

`

`

`csharp
internal bool GetConflictedFieldMemberTypeId(TypeDefinitionBase typeDefinition, ref MemberTypeId memberTypeId)
internal bool GetConflictedPropertyMemberTypeId(TypeDefinitionBase typeDefinition, ref MemberTypeId memberTypeId)
`

`

`

在跨版本加载时，通过 

`IConflictResolver

` 将旧字段/属?ID 映射到新 ID。仅当启用了冲突解决?resolver 适用于当前操作版本时生效?
### GenerateCode

`

`

`csharp
public void GenerateCode(SaveCodeGenerationContext context)
`

`

`

将所有类、结构体、容器定义按程序集分组添加到代码生成上下文中，然后调?

`context.FillFiles()

` 生成代码文件。用于存档系统的代码生成工具?
### AddClassDefinition / AddStructDefinition / AddInterfaceDefinition / AddEnumDefinition / AddContainerDefinition / AddBasicTypeDefinition / AddRootClassDefinition

`

`

`csharp
internal void AddRootClassDefinition(TypeDefinition rootClassDefinition)
internal void AddClassDefinition(TypeDefinition classDefinition)
internal void AddStructDefinition(StructDefinition structDefinition)
internal void AddInterfaceDefinition(InterfaceDefinition interfaceDefinition)
internal void AddEnumDefinition(EnumDefinition enumDefinition)
internal void AddContainerDefinition(ContainerDefinition containerDefinition)
internal void AddBasicTypeDefinition(BasicTypeDefinition basicTypeDefinition)
`

`

`

将类型定义注册到对应的类型字典和 

`SaveId

` 字典中，同时注册?

`_allTypeDefinitions

` ?

`_allTypeDefinitionsWithId

`。这些方法由 

`SaveableTypeDefiner

` 在定义阶段调用?
### AddConflictResolver

`

`

`csharp
internal void AddConflictResolver(TypeSaveId saveId, IConflictResolver conflictResolver)
`

`

`

注册冲突解决器，同时建立 

`SaveId ?resolver

` ?

`TypeDefinition ?resolver

` 两套索引?
## 使用示例

### 自定?SaveableTypeDefiner

`

`

`csharp
using TaleWorlds.SaveSystem;
using TaleWorlds.SaveSystem.Definition;

// DefinitionContext 会自动发现并初始化所?SaveableTypeDefiner 子类
public class MyModTypeDefiner : SaveableTypeDefiner
{
    public MyModTypeDefiner() : base(500000) { }

    protected override void DefineClassTypes()
    {
        // 注册自定义可存档类，分配唯一 ID
        AddClassDefinition(typeof(MySaveableClass), 1, null);
    }

    protected override void DefineContainerDefinitions()
    {
        // 注册自定义容?        AddContainerDefinition(typeof(List&lt;MySaveableClass&gt;), 1);
    }
}
`

`

`

### 查询类型定义（内部使用）

`

`

`csharp
// DefinitionContext 主要?SaveManager 内部使用
// 开发者通常不直接调用，但了解其工作原理有助于调试存档问?// 若存档加载时出现类型找不到的错误，通常?TypeDefiner 未正确注?

`

`

`

## 参见

- [完整类目录](../catalog)
- [本领域目录](../catalog-save)
