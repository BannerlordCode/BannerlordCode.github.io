---
title: "StrategicArea"
description: "Auto-generated class reference for StrategicArea."
---
# StrategicArea

**Namespace:** TaleWorlds.MountAndBlade
**Module:** TaleWorlds.MountAndBlade
**Type:** `public class StrategicArea : MissionObject, IDetachment`
**Base:** `MissionObject`
**File:** `bin/TaleWorlds.MountAndBlade/TaleWorlds.MountAndBlade/StrategicArea.cs`

## Overview

`StrategicArea` lives in `TaleWorlds.MountAndBlade` and exposes the state, behavior, or workflow entry points of that subsystem to mod developers through its public members. Read its properties as “what state it owns” and its methods as “what actions it allows”.

## Mental Model

Start from namespace `TaleWorlds.MountAndBlade` to place it in the stack, then inspect its public methods: if it mainly exposes Get/Set members, it is likely a state object; if it centers on Create/Apply/Execute verbs, it behaves more like a service or workflow entry point.

## Key Properties

| Name | Signature |
|------|-----------|
| `IsActive` | `public bool IsActive { get; set; }` |

## Key Methods

### GetGroundPosition
`public Vec3 GetGroundPosition()`

**Purpose:** **Purpose:** Reads and returns the ground position value held by the this instance.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.GetGroundPosition();
```

### DetermineAssociatedDestructibleComponents
`public void DetermineAssociatedDestructibleComponents(IEnumerable<DestructableComponent> destructibleComponents)`

**Purpose:** **Purpose:** Determines the result of associated destructible components based on the current state.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
strategicArea.DetermineAssociatedDestructibleComponents(destructibleComponents);
```

### OnParentGameEntityVisibilityChanged
`public void OnParentGameEntityVisibilityChanged(bool isVisible)`

**Purpose:** **Purpose:** Invoked when the parent game entity visibility changed event is raised.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
strategicArea.OnParentGameEntityVisibilityChanged(false);
```

### InitializeAutogenerated
`public void InitializeAutogenerated(float width, int capacity, BattleSideEnum side)`

**Purpose:** **Purpose:** Prepares the resources, state, or bindings required by autogenerated.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
strategicArea.InitializeAutogenerated(0, 0, side);
```

### AddAgent
`public void AddAgent(Agent agent, int slotIndex = -1, Agent.AIScriptedFrameFlags customFlags = Agent.AIScriptedFrameFlags.None)`

**Purpose:** **Purpose:** Adds agent to the current collection or state.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
strategicArea.AddAgent(agent, 0, agent.AIScriptedFrameFlags.None);
```

### AddAgentAtSlotIndex
`public void AddAgentAtSlotIndex(Agent agent, int slotIndex = -1)`

**Purpose:** **Purpose:** Adds agent at slot index to the current collection or state.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
strategicArea.AddAgentAtSlotIndex(agent, 0);
```

### IsUsedByFormation
`public bool IsUsedByFormation(Formation formation)`

**Purpose:** **Purpose:** Determines whether the this instance is in the used by formation state or condition.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.IsUsedByFormation(formation);
```

### IsAgentEligible
`public bool IsAgentEligible(Agent agent)`

**Purpose:** **Purpose:** Determines whether the this instance is in the agent eligible state or condition.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.IsAgentEligible(agent);
```

### IsStandingPointAvailableForAgent
`public bool IsStandingPointAvailableForAgent(Agent agent)`

**Purpose:** **Purpose:** Determines whether the this instance is in the standing point available for agent state or condition.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.IsStandingPointAvailableForAgent(agent);
```

### GetTemplateCostsOfAgent
`public List<float> GetTemplateCostsOfAgent(Agent candidate, List<float> oldValue)`

**Purpose:** **Purpose:** Reads and returns the template costs of agent value held by the this instance.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.GetTemplateCostsOfAgent(candidate, oldValue);
```

### GetTemplateWeightOfAgent
`public float GetTemplateWeightOfAgent(Agent candidate)`

**Purpose:** **Purpose:** Reads and returns the template weight of agent value held by the this instance.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.GetTemplateWeightOfAgent(candidate);
```

### GetWeightOfAgentAtNextSlot
`public float? GetWeightOfAgentAtNextSlot(List<Agent> newAgents, out Agent match)`

**Purpose:** **Purpose:** Reads and returns the weight of agent at next slot value held by the this instance.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.GetWeightOfAgentAtNextSlot(newAgents, match);
```

### GetWeightOfAgentAtNextSlot
`public float? GetWeightOfAgentAtNextSlot(List<(Agent, float)> agentTemplateScores, out Agent match)`

**Purpose:** **Purpose:** Reads and returns the weight of agent at next slot value held by the this instance.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.GetWeightOfAgentAtNextSlot(list<(Agent, 0, match);
```

### GetWeightOfAgentAtOccupiedSlot
`public float? GetWeightOfAgentAtOccupiedSlot(Agent detachedAgent, List<Agent> newAgents, out Agent match)`

**Purpose:** **Purpose:** Reads and returns the weight of agent at occupied slot value held by the this instance.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.GetWeightOfAgentAtOccupiedSlot(detachedAgent, newAgents, match);
```

### RemoveAgent
`public void RemoveAgent(Agent agent)`

**Purpose:** **Purpose:** Removes agent from the current collection or state.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
strategicArea.RemoveAgent(agent);
```

### GetNumberOfUsableSlots
`public int GetNumberOfUsableSlots()`

**Purpose:** **Purpose:** Reads and returns the number of usable slots value held by the this instance.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.GetNumberOfUsableSlots();
```

### GetAgentFrame
`public WorldFrame? GetAgentFrame(Agent agent)`

**Purpose:** **Purpose:** Reads and returns the agent frame value held by the this instance.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.GetAgentFrame(agent);
```

### GetWeightOfNextSlot
`public float? GetWeightOfNextSlot(BattleSideEnum side)`

**Purpose:** **Purpose:** Reads and returns the weight of next slot value held by the this instance.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.GetWeightOfNextSlot(side);
```

### GetWeightOfOccupiedSlot
`public float GetWeightOfOccupiedSlot(Agent agent)`

**Purpose:** **Purpose:** Reads and returns the weight of occupied slot value held by the this instance.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.GetWeightOfOccupiedSlot(agent);
```

### IsUsableBy
`public bool IsUsableBy(BattleSideEnum side)`

**Purpose:** **Purpose:** Determines whether the this instance is in the usable by state or condition.

```csharp
// Obtain an instance of StrategicArea from the subsystem API first
StrategicArea strategicArea = ...;
var result = strategicArea.IsUsableBy(side);
```

## Usage Example

```csharp
// Typically call this after obtaining an instance from the subsystem API
StrategicArea strategicArea = ...;
strategicArea.GetGroundPosition();
```

## See Also

- [Area Index](../)