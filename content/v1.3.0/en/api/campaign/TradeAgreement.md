---
title: "TradeAgreement"
description: "Auto-generated class reference for TradeAgreement."
---
# TradeAgreement

**Namespace:** TaleWorlds.CampaignSystem.CampaignBehaviors
**Module:** TaleWorlds.CampaignSystem
**Type:** `public class TradeAgreement`
**Base:** none
**File:** `TaleWorlds.CampaignSystem/CampaignBehaviors/TradeAgreementsCampaignBehavior.cs`

## Overview

`TradeAgreement` lives in `TaleWorlds.CampaignSystem.CampaignBehaviors` and exposes the state, behavior, or workflow entry points of that subsystem to mod developers through its public members. Read its properties as “what state it owns” and its methods as “what actions it allows”.

## Mental Model

Start from namespace `TaleWorlds.CampaignSystem.CampaignBehaviors` to place it in the stack, then inspect its public methods: if it mainly exposes Get/Set members, it is likely a state object; if it centers on Create/Apply/Execute verbs, it behaves more like a service or workflow entry point.

## Key Methods

### RegisterEvents
`public override void RegisterEvents()`

**Purpose:** Registers `events` with the current system so it can later be observed or dispatched.

```csharp
// Obtain an instance of TradeAgreement from the subsystem API first
TradeAgreement tradeAgreement = ...;
tradeAgreement.RegisterEvents();
```

### OnTradeAgreementOfferedToPlayer
`public void OnTradeAgreementOfferedToPlayer(Kingdom fromKingdom)`

**Purpose:** Invoked when the `trade agreement offered to player` event is raised.

```csharp
// Obtain an instance of TradeAgreement from the subsystem API first
TradeAgreement tradeAgreement = ...;
tradeAgreement.OnTradeAgreementOfferedToPlayer(fromKingdom);
```

### SyncData
`public override void SyncData(IDataStore dataStore)`

**Purpose:** Synchronizes `data` across the relevant contexts or systems.

```csharp
// Obtain an instance of TradeAgreement from the subsystem API first
TradeAgreement tradeAgreement = ...;
tradeAgreement.SyncData(dataStore);
```

### MakeTradeAgreement
`public void MakeTradeAgreement(Kingdom kingdom1, Kingdom kingdom2, CampaignTime duration)`

**Purpose:** Performs the operation described by this method.

```csharp
// Obtain an instance of TradeAgreement from the subsystem API first
TradeAgreement tradeAgreement = ...;
tradeAgreement.MakeTradeAgreement(kingdom1, kingdom2, duration);
```

### EndTradeAgreementsOfKingdom
`public void EndTradeAgreementsOfKingdom(Kingdom kingdom)`

**Purpose:** Performs the operation described by this method.

```csharp
// Obtain an instance of TradeAgreement from the subsystem API first
TradeAgreement tradeAgreement = ...;
tradeAgreement.EndTradeAgreementsOfKingdom(kingdom);
```

### EndTradeAgreement
`public void EndTradeAgreement(Kingdom kingdom1, Kingdom kingdom2)`

**Purpose:** Performs the operation described by this method.

```csharp
// Obtain an instance of TradeAgreement from the subsystem API first
TradeAgreement tradeAgreement = ...;
tradeAgreement.EndTradeAgreement(kingdom1, kingdom2);
```

### HasTradeAgreement
`public bool HasTradeAgreement(Kingdom kingdom1, Kingdom kingdom2, out CampaignTime endTime)`

**Purpose:** Determines whether the current object already holds `trade agreement`.

```csharp
// Obtain an instance of TradeAgreement from the subsystem API first
TradeAgreement tradeAgreement = ...;
var result = tradeAgreement.HasTradeAgreement(kingdom1, kingdom2, endTime);
```

### AutoGeneratedStaticCollectObjectsTradeAgreement
`public static void AutoGeneratedStaticCollectObjectsTradeAgreement(object o, List<object> collectedObjects)`

**Purpose:** Performs the operation described by this method.

```csharp
// Static call; no instance required
TradeAgreement.AutoGeneratedStaticCollectObjectsTradeAgreement(o, collectedObjects);
```

## Usage Example

```csharp
// Typically call this after obtaining an instance from the subsystem API
TradeAgreement tradeAgreement = ...;
tradeAgreement.RegisterEvents();
```

## See Also

- [Area Index](../)