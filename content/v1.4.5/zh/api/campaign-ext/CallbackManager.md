---
title: "CallbackManager"
---
<!-- BEGIN BREADCRUMB -->
**首页** → **API 目录** → **本领域** → `CallbackManager`
- [← 本领域 / 返回 campaign-ext](./)
- [↑ API 目录](../)
- [🏠 首页 v1.4.5](../../)
- [⭐ 版本架构](../../architecture/)
<!-- END BREADCRUMB -->
# CallbackManager

**Namespace:** ManagedCallbacks
**Module:** ManagedCallbacks
**Type:** `public class CallbackManager : ICallbackManager`
**Base:** `ICallbackManager`
**File:** `Bannerlord.Source/bin/TaleWorlds.DotNet.AutoGenerated/ManagedCallbacks/CallbackManager.cs`

## 概述

`CallbackManager` 是一个管理器：它拥有子系统的生命周期、查找入口和跨对象协调职责。

## 心智模型

把 `CallbackManager` 当作一个 Manager 型扩展点来理解：先确认谁创建它、谁持有它、谁调用它，再决定是继承、组合还是只读使用。

## 主要方法

### Initialize
`public void Initialize()`

**用途 / Purpose:** 初始化 `initialize` 的状态、资源或绑定。

### GetDelegates
`public Delegate GetDelegates()`

**用途 / Purpose:** 获取 `delegates` 的当前值。

### GetScriptingInterfaceObjects
`public Dictionary<string, object> GetScriptingInterfaceObjects()`

**用途 / Purpose:** 获取 `scripting interface objects` 的当前值。

### SetFunctionPointer
`public void SetFunctionPointer(int id, IntPtr pointer)`

**用途 / Purpose:** 设置 `function pointer` 的值或状态。

### CheckSharedStructureSizes
`public void CheckSharedStructureSizes()`

**用途 / Purpose:** 处理 `check shared structure sizes` 相关逻辑。

## 使用示例

```csharp
var manager = CallbackManager.Current;
```

## 参见

- [完整类目录](../catalog)