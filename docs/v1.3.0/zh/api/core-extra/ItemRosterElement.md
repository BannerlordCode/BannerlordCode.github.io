<!-- BEGIN BREADCRUMB -->
**首页** → **API 目录** → **本领域** → `ItemRosterElement`
- [← 本领域 / 返回 core-extra](./)
- [↑ API 目录](../)
- [🏠 首页 v1.3.0](../../)
- [⭐ 版本架构](../../architecture/)
<!-- END BREADCRUMB -->
# ItemRosterElement

**Namespace:** TaleWorlds.Core
**Module:** TaleWorlds.Core
**Type:** `public struct ItemRosterElement : ISerializableObject, ISavedStruct`
**Base:** `ISerializableObject`
**File:** `TaleWorlds.Core/ItemRosterElement.cs`

## 概述

`ItemRosterElement` 位于 `TaleWorlds.Core`，它通过这组公开成员把对应子系统的状态、行为或流程入口暴露给 mod 开发者。阅读时先看属性代表“它持有什么状态”，再看方法代表“它允许你做什么”。

## 心智模型

先从命名空间 `TaleWorlds.Core` 判断它属于哪层系统，再看公开方法：如果以 Get/Set 为主，它多半是状态对象；如果以 Create/Apply/Execute 为主，它更像服务或流程入口。

## 主要属性

| Name | Signature |
|------|-----------|
| `Invalid` | `public static ItemRosterElement Invalid { get; }` |
| `EquipmentElement` | `public EquipmentElement EquipmentElement { get; }` |
| `Amount` | `public int Amount { get; set; }` |
| `IsEmpty` | `public bool IsEmpty { get; }` |

## 主要方法

### AutoGeneratedStaticCollectObjectsItemRosterElement
`public static void AutoGeneratedStaticCollectObjectsItemRosterElement(object o, List<object> collectedObjects)`

**用途 / Purpose:** 处理 `auto generated static collect objects item roster element` 相关逻辑。

### GetHashCode
`public override int GetHashCode()`

**用途 / Purpose:** 获取 `hash code` 的当前值。

### ToString
`public override string ToString()`

**用途 / Purpose:** 处理 `to string` 相关逻辑。

### Clear
`public void Clear()`

**用途 / Purpose:** 处理 `clear` 相关逻辑。

### IsEqualTo
`public bool IsEqualTo(ItemRosterElement other)`

**用途 / Purpose:** 处理 `is equal to` 相关逻辑。

### Equals
`public override bool Equals(object obj)`

**用途 / Purpose:** 处理 `equals` 相关逻辑。

### GetRosterElementWeight
`public float GetRosterElementWeight()`

**用途 / Purpose:** 获取 `roster element weight` 的当前值。

## 使用示例

```csharp
ItemRosterElement.AutoGeneratedStaticCollectObjectsItemRosterElement(o, collectedObjects);
```

## 参见

- [完整类目录](../catalog)