# CampaignMission Lifecycle Wave Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the ten v1.4.5 CampaignMission lifecycle shells with bilingual, source-backed pages that explain mission entry routing, SandBox ownership, `CampaignMission.Current` lifetime, and safe campaign/mission boundaries.

**Architecture:** Keep the existing clean URLs and section placement. Treat `CampaignMission.cs` and `ICampaignMission.cs` as the CampaignSystem contract, `Campaign.cs` and `SandBoxSubModule.cs` as ownership/injection evidence, `SandBox.CampaignMissionManager` as the forwarding adapter, and `CampaignMissionComponent` plus `SandBoxMissions` as the mission lifetime implementation. Navigation remains generated from the existing content tree; this wave does not hand-edit navigation artifacts.

**Tech Stack:** Markdown, Zola, Node.js audit scripts, PowerShell, Bannerlord v1.4.5 decompiled C# source.

## Global Constraints

- All product prose is handwritten from source and call sites; no signature-to-prose generator or retired body writer.
- Preserve the dirty shared worktree; do not reset, clean, stash, checkout, commit, or overwrite unrelated changes.
- Keep the ten existing routes and maintain zh/en parity of the mental model, dependencies, examples, risks, and navigation.
- Use real acquisition paths such as `Campaign.Current.CampaignMissionManager`, `CampaignMission.Current`, `Mission.Current`, and `MissionState.OpenNew`; never use ellipses, `SomeValue`, or fake `service = ...` acquisition.
- Use relative clean-URL links and distinguish the CampaignSystem contract from the SandBox implementation.
- Report scoped evidence separately from the still-incomplete global R1/H0-H10 objective and do not claim a Zola build pass on mapped `R:` without supported evidence.

---

### Task 1: Freeze scope and source contract

**Files:**
- Read: `bannerlord-1.4.5/Bannerlord.Source/bin/TaleWorlds.CampaignSystem/TaleWorlds.CampaignSystem/CampaignMission.cs`
- Read: `bannerlord-1.4.5/Bannerlord.Source/bin/TaleWorlds.CampaignSystem/TaleWorlds.CampaignSystem/ICampaignMission.cs`
- Read: `bannerlord-1.4.5/Bannerlord.Source/Modules.SandBox/SandBox/Sandbox/CampaignMissionManager.cs`
- Read: `bannerlord-1.4.5/Bannerlord.Source/Modules.SandBox/SandBox/SandBox.Missions.MissionLogics/CampaignMissionComponent.cs`
- Read: `bannerlord-1.4.5/Bannerlord.Source/Modules.SandBox/SandBox/Sandbox/SandBoxMissions.cs`
- Read: `bannerlord-1.4.5/Bannerlord.Source/Modules.SandBox/SandBox/Sandbox/SandBoxSubModule.cs`

**Interfaces:**
- Produces: a frozen ten-page scope and a source chain for the prose and examples.

- [x] **Step 1: Verify the existing routes and dirty-tree boundary**

Run from `R:\Bannerlord\BannerlordCode.github.io`:

```powershell
git status --short
Test-Path content/v1.4.5/zh/api/campaign/CampaignMission.md
Test-Path content/v1.4.5/en/api/campaign-ext/CampaignMissionComponent.md
```

Expected: existing unrelated modifications remain untouched and all ten target routes exist.

- [x] **Step 2: Verify the lifecycle chain**

Check that `Campaign.cs` owns `CampaignMissionManager`, `SandBoxSubModule.OnGameInitializationFinished` injects `new CampaignMissionManager()`, the adapter forwards to `SandBoxMissions`, and the component sets/clears `CampaignMission.Current` in `OnCreated`/`OnEndMission`.

Expected: these facts are present in the source before any page body is rewritten.

### Task 2: Rewrite the CampaignSystem contract pages

**Files:**
- Modify: `content/v1.4.5/zh/api/campaign/CampaignMission.md`
- Modify: `content/v1.4.5/en/api/campaign/CampaignMission.md`
- Modify: `content/v1.4.5/zh/api/campaign/ICampaignMission.md`
- Modify: `content/v1.4.5/en/api/campaign/ICampaignMission.md`
- Modify: `content/v1.4.5/zh/api/campaign/ICampaignMissionManager.md`
- Modify: `content/v1.4.5/en/api/campaign/ICampaignMissionManager.md`

**Interfaces:**
- Consumes: Task 1's CampaignSystem source contract.
- Produces: handwritten public entry documentation that points to the SandBox implementation without pretending the interface creates missions itself.

- [x] **Step 1: Replace the `CampaignMission` shell**

Document `Current`, its static forwarding role, the overload families, the `Campaign.Current` prerequisite, and the difference between opening a mission and attaching mission behaviors. Include a real `Campaign.Current` acquisition example and links to the interface, implementation, `Mission`, and campaign lifecycle pages.

- [x] **Step 2: Replace the `ICampaignMission` shell**

Document the mission-scoped state and conversation/location/following hooks, including that the public implementation is the `CampaignMissionComponent` attached by SandBox mission initializers. Include a `Mission.Current.GetMissionBehavior<CampaignMissionComponent>()` acquisition path with a null-safe check.

- [x] **Step 3: Replace the `ICampaignMissionManager` shell**

Group its real entries by battle, settlement, conversation, and special mission. Explain explicit interface dispatch and why mods normally call `CampaignMission` rather than construct the manager. Include the manager ownership chain and a real `Campaign.Current.CampaignMissionManager` read-only inspection example.

### Task 3: Rewrite the SandBox implementation pages

**Files:**
- Modify: `content/v1.4.5/zh/api/campaign-ext/CampaignMissionManager.md`
- Modify: `content/v1.4.5/en/api/campaign-ext/CampaignMissionManager.md`
- Modify: `content/v1.4.5/zh/api/campaign-ext/CampaignMissionComponent.md`
- Modify: `content/v1.4.5/en/api/campaign-ext/CampaignMissionComponent.md`

**Interfaces:**
- Consumes: Task 1's SandBox source and mission initializer call sites.
- Produces: handwritten implementation pages covering the adapter boundary and mission behavior lifetime.

- [x] **Step 1: Replace `CampaignMissionManager`**

Explain the `SandBox` implementation, `CampaignMissionManager` injection during campaign initialization, explicit forwarding to `SandBoxMissions`, and the three naval methods that currently return `null` in this source. Do not invent a `Current` property or promise naval support. Use a real campaign manager acquisition path.

- [x] **Step 2: Replace `CampaignMissionComponent`**

Explain its `MissionLogic` lifecycle, `ICampaignMission` implementation, `OnCreated` assignment, `OnBehaviorInitialize` event dispatch, `OnMissionResultReady` battle-result handoff, and `OnEndMission` cleanup. Cover conversation animation/sound, location transitions, agent following, `AgentSupplier`, `Location`, `LastVisitedAlley`, and `Mode` without turning private implementation details into a signature wall.

- [x] **Step 3: Add bilingual navigation and risk sections**

Every page must have parent, sibling, related links, explicit dependency links, a real example, and crash/save/lifecycle warnings. Confirm every target route exists before using it.

### Task 4: Run scoped gates and record evidence

**Files:**
- Read/verify: all ten target pages and generated navigation artifacts.
- Create: `.superpowers/sdd/2026-08-04-v1.4.5-campaign-mission-wave/progress.md`

**Interfaces:**
- Consumes: rewritten pages from Tasks 2-3.
- Produces: reproducible scoped gate results and an honest global boundary report.

- [x] **Step 1: Run page-level quality and forbidden-token checks**

Run the existing quality audit for all ten target files and scan only those files for `自动生成`, `Auto-generated`, `SomeValue`, `service = ...`, `IICampaignMission`, and boilerplate mental-model fragments.

Expected: zero blockers and zero forbidden-token hits in the target scope.

- [x] **Step 2: Run structural gates serially**

Run:

```powershell
npm test
node tools/audit-links.mjs
node tools/audit-doc-quality.mjs content
node tools/audit-body-writers.mjs
node tools/audit-navigation.mjs
node tools/generate-section-tree.mjs --check
node --max-old-space-size=8192 tools/generate-page-navigation.mjs --check
```

Expected: record exact outputs; any pre-existing unrelated failure must be separated from target-page evidence.

- [x] **Step 3: Record the wave boundary**

Write the progress ledger with target files, source chain, quality/link/navigation results, coverage impact if freshly measurable, manual scenario status, and the known mapped-`R:` build limitation. Keep the global goal active unless every H0-H10 requirement is actually proven.
