<!-- BEGIN BREADCRUMB -->
**首页** → **API 目录** → **本领域** → `MBGUID`
- [← 本领域 / 返回 campaign-ext](./)
- [↑ API 目录](../)
- [⭐ SDK 总览](../../architecture/sdk-overview)
<!-- END BREADCRUMB -->
# MBGUID

**命名空间:** TaleWorlds.ObjectSystem
**模块:** TaleWorlds.ObjectSystem
**类型:** 结构体 struct struct
**领域:** 战役系统 Campaign

## 概述

> 本页为自动生成的存根。`MBGUID` 是 `TaleWorlds.ObjectSystem` 命名空间下的一个结构体 struct。
> 如需了解其属性、方法和开发者用例，请参考源码或
## 主要属性

| Name | Signature |
|------|-----------|
| `InternalValue` | `public uint InternalValue { get; }` |
| `SubId` | `public uint SubId { get; }` |


## 主要方法

### GetHash2

```csharp
public static long GetHash2(MBGUID id1, MBGUID id2)
```

### CompareTo

```csharp
public int CompareTo(object a)
```

### GetTypeIndex

```csharp
public uint GetTypeIndex()
```

### GetHashCode

```csharp
public override int GetHashCode()
```

### ToString

```csharp
public override string ToString()
```

### Equals

```csharp
public override bool Equals(object obj)
```

### Equals

```csharp
public bool Equals(MBGUID other)
```

贡献文档。

## 参见

- [完整类目录](../catalog)
- [本领域目录](../catalog-campaign)