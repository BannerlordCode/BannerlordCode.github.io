---
title: "TradeSkillCampaignBehavior"
description: "Auto-generated class reference for TradeSkillCampaignBehavior."
---
# TradeSkillCampaignBehavior

**Namespace:** TaleWorlds.CampaignSystem.CampaignBehaviors
**Module:** TaleWorlds.CampaignSystem
**Type:** `public class TradeSkillCampaignBehavior : CampaignBehaviorBase, IPlayerTradeBehavior`
**Base:** `CampaignBehaviorBase`
**File:** `TaleWorlds.CampaignSystem/CampaignBehaviors/TradeSkillCampaignBehavior.cs`

## Overview

`TradeSkillCampaignBehavior` lives in `TaleWorlds.CampaignSystem.CampaignBehaviors` and exposes the state, behavior, or workflow entry points of that subsystem to mod developers through its public members. Read its properties as “what state it owns” and its methods as “what actions it allows”.

## Mental Model

Start from namespace `TaleWorlds.CampaignSystem.CampaignBehaviors` to place it in the stack, then inspect its public methods: if it mainly exposes Get/Set members, it is likely a state object; if it centers on Create/Apply/Execute verbs, it behaves more like a service or workflow entry point.

## Key Methods

### RegisterEvents
`public override void RegisterEvents()`

**Purpose:** **Purpose:** Registers events with the current system so it can later be observed or dispatched.

```csharp
// Obtain an instance of TradeSkillCampaignBehavior from the subsystem API first
TradeSkillCampaignBehavior tradeSkillCampaignBehavior = ...;
tradeSkillCampaignBehavior.RegisterEvents();
```

### SyncData
`public override void SyncData(IDataStore dataStore)`

**Purpose:** **Purpose:** Synchronizes data across the relevant contexts or systems.

```csharp
// Obtain an instance of TradeSkillCampaignBehavior from the subsystem API first
TradeSkillCampaignBehavior tradeSkillCampaignBehavior = ...;
tradeSkillCampaignBehavior.SyncData(dataStore);
```

### GetProjectedProfit
`public int GetProjectedProfit(ItemRosterElement itemRosterElement, int itemCost)`

**Purpose:** **Purpose:** Reads and returns the projected profit value held by the this instance.

```csharp
// Obtain an instance of TradeSkillCampaignBehavior from the subsystem API first
TradeSkillCampaignBehavior tradeSkillCampaignBehavior = ...;
var result = tradeSkillCampaignBehavior.GetProjectedProfit(itemRosterElement, 0);
```

### AutoGeneratedStaticCollectObjectsItemTradeData
`public static void AutoGeneratedStaticCollectObjectsItemTradeData(object o, List<object> collectedObjects)`

**Purpose:** **Purpose:** Executes the AutoGeneratedStaticCollectObjectsItemTradeData logic.

```csharp
// Static call; no instance required
TradeSkillCampaignBehavior.AutoGeneratedStaticCollectObjectsItemTradeData(o, collectedObjects);
```

## Usage Example

```csharp
// Typically call this after obtaining an instance from the subsystem API
TradeSkillCampaignBehavior tradeSkillCampaignBehavior = ...;
tradeSkillCampaignBehavior.RegisterEvents();
```

## See Also

- [Area Index](../)