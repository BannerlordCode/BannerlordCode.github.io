<!-- BEGIN BREADCRUMB -->
**Home** → **API Index** → **Area** → `CampaignVec2`
- [← Area / Back to campaign](./)
- [↑ API Index](../)
- [🏠 Home v1.3.0](../../)
- [⭐ Version Architecture](../../architecture/)
<!-- END BREADCRUMB -->
# CampaignVec2

**Namespace:** TaleWorlds.CampaignSystem
**Module:** TaleWorlds.CampaignSystem
**Type:** `public struct CampaignVec2 : ISavedStruct`
**Base:** `ISavedStruct`
**File:** `TaleWorlds.CampaignSystem/CampaignVec2.cs`

## Overview

`CampaignVec2` lives in `TaleWorlds.CampaignSystem` and exposes the state, behavior, or workflow entry points of that subsystem to mod developers through its public members. Read its properties as “what state it owns” and its methods as “what actions it allows”.

## Mental Model

Start from namespace `TaleWorlds.CampaignSystem` to place it in the stack, then inspect its public methods: if it mainly exposes Get/Set members, it is likely a state object; if it centers on Create/Apply/Execute verbs, it behaves more like a service or workflow entry point.

## Key Properties

| Name | Signature |
|------|-----------|
| `Invalid` | `public static CampaignVec2 Invalid { get; }` |
| `Zero` | `public static CampaignVec2 Zero { get; }` |
| `X` | `public float X { get; }` |
| `Y` | `public float Y { get; }` |
| `Face` | `public PathFaceRecord Face { get; }` |
| `Length` | `public float Length { get; }` |
| `LengthSquared` | `public float LengthSquared { get; }` |
| `RotationInRadians` | `public float RotationInRadians { get; }` |

## Key Methods

### AutoGeneratedStaticCollectObjectsCampaignVec2
`public static void AutoGeneratedStaticCollectObjectsCampaignVec2(object o, List<object> collectedObjects)`

**Purpose:** Handles logic related to `auto generated static collect objects campaign vec2`.

### AddVec2
`public void AddVec2(Vec2 vec2)`

**Purpose:** Adds `vec2` to the current collection or state.

### ToVec2
`public Vec2 ToVec2()`

**Purpose:** Handles logic related to `to vec2`.

### Equals
`public override bool Equals(object obj)`

**Purpose:** Handles logic related to `equals`.

### AsVec3
`public Vec3 AsVec3()`

**Purpose:** Handles logic related to `as vec3`.

### Equals
`public bool Equals(CampaignVec2 other)`

**Purpose:** Handles logic related to `equals`.

### IsValid
`public bool IsValid()`

**Purpose:** Handles logic related to `is valid`.

### DistanceSquared
`public float DistanceSquared(CampaignVec2 v)`

**Purpose:** Handles logic related to `distance squared`.

### DistanceSquared
`public float DistanceSquared(Vec2 v)`

**Purpose:** Handles logic related to `distance squared`.

### Distance
`public float Distance(CampaignVec2 v)`

**Purpose:** Handles logic related to `distance`.

### Distance
`public float Distance(Vec2 v)`

**Purpose:** Handles logic related to `distance`.

### ToString
`public override string ToString()`

**Purpose:** Handles logic related to `to string`.

### IsDefault
`public bool IsDefault()`

**Purpose:** Handles logic related to `is default`.

### IsNonZero
`public bool IsNonZero()`

**Purpose:** Handles logic related to `is non zero`.

### NearlyEquals
`public bool NearlyEquals(Vec2 v, float epsilon = 1E-05f)`

**Purpose:** Handles logic related to `nearly equals`.

### NearlyEquals
`public bool NearlyEquals(CampaignVec2 v, float epsilon = 1E-05f)`

**Purpose:** Handles logic related to `nearly equals`.

### Normalize
`public float Normalize()`

**Purpose:** Handles logic related to `normalize`.

### Normalized
`public static CampaignVec2 Normalized(CampaignVec2 v)`

**Purpose:** Handles logic related to `normalized`.

### RightVec
`public CampaignVec2 RightVec()`

**Purpose:** Handles logic related to `right vec`.

### LeftVec
`public CampaignVec2 LeftVec()`

**Purpose:** Handles logic related to `left vec`.

### GetHashCode
`public override int GetHashCode()`

**Purpose:** Gets the current value of `hash code`.

## Usage Example

```csharp
CampaignVec2.AutoGeneratedStaticCollectObjectsCampaignVec2(o, collectedObjects);
```

## See Also

- [Complete Class Catalog](../catalog)