---
title: 存档系统指南 / Save System Guide
description: v1.3.15 全新存档系统使用指南 / Complete guide to v1.3.15's new SaveSystem
---

# 存档系统指南 / Save System Guide

**Namespace**: TaleWorlds.SaveSystem
**Version**: 1.3.15 (NEW - 不存在于 1.3.0)

## 概述 / Overview

TaleWorlds.SaveSystem 是 v1.3.15 引入的全新存档系统。在 v1.3.0 中，存档逻辑分散在各个模块，而 v1.3.15 将其统一到一个专门的系统中。

TaleWorlds.SaveSystem is a new save system introduced in v1.3.15. In v1.3.0, save logic was scattered across modules, but v1.3.15 unified it into a dedicated system.

## 目录 / Contents

1. [核心概念](#核心概念)
2. [创建可存档类](#创建可存档类)
3. [SaveManager 用法](#savemanager-用法)
4. [常见问题](#常见问题)
5. [相关 API](#相关-api)

---

## 核心概念

### 存档属性

| 属性 | 说明 |
|------|------|
| `[SaveableRootClass(id)]` | 标记为存档根类 |
| `[SaveableClass(id)]` | 标记为可存档的类 |
| `[SaveableField(id)]` | 标记可存档的字段 |
| `[SaveableProperty(id)]` | 标记可存档的属性 |

### 基本类型

```csharp
// 简单类型（自动支持）
[SaveableField(1)] public int IntValue;
[SaveableField(2)] public float FloatValue;
[SaveableField(3)] public string StringValue;
[SaveableField(4)] public bool BoolValue;

// 数组和容器
[SaveableField(5)] public int[] IntArray;
[SaveableField(6)] public List<string> StringList;
[SaveableField(7)] public Dictionary<string, int> StringIntDict;
```

---

## 创建可存档类

### 简单示例

```csharp
using TaleWorlds.SaveSystem;

namespace MyModule
{
    // 标记为可存档类，ID 为 1
    [SaveableClass(1)]
    public class MyData
    {
        // 标记可存档字段
        [SaveableField(1)]
        public int Score;
        
        [SaveableField(2)]
        public string PlayerName;
        
        [SaveableField(3)]
        public List<Item> Inventory;
    }
}
```

### 嵌套类

```csharp
[SaveableClass(2)]
public class Item
{
    [SaveableField(1)]
    public string Name;
    
    [SaveableField(2)]
    public int Quantity;
}

[SaveableClass(1)]
public class MyData
{
    [SaveableField(1)]
    public Item EquippedItem;
    
    [SaveableField(2)]
    public List<Item> Inventory;
}
```

### 根类

```csharp
// 存档根类需要有唯一的类型 ID
[SaveableRootClass(5000)]
public class MyModData
{
    [SaveableField(1)]
    public MyData GameData;
    
    [SaveableField(2)]
    public int Version;
}
```

---

## SaveManager 用法

### 保存游戏

```csharp
using TaleWorlds.SaveSystem;

// 创建数据
MyModData data = new MyModData
{
    GameData = new MyData { Score = 100, PlayerName = "Hero" },
    Version = 1
};

// 创建元数据
MetaData metaData = new MetaData
{
    SaveName = "MySave",
    GameVersion = ApplicationVersion.FromParametersFile(null),
    CreationTime = DateTime.Now
};

// 使用 SaveDriver 保存
ISaveDriver driver = new AsyncFileSaveDriver();
SaveOutput result = SaveManager.Save(data, metaData, "MySave", driver);

if (result.Succeeded)
{
    Debug.Print("Save successful!");
}
```

### 加载游戏

```csharp
// 使用 SaveDriver 加载
ISaveDriver driver = new AsyncFileSaveDriver();
LoadResult loadResult = SaveManager.Load("MySave", driver);

if (loadResult.Succeeded)
{
    MyModData data = loadResult.RootObject as MyModData;
    Debug.Print($"Loaded: {data.GameData.PlayerName}");
}
```

### 自定义 SaveDriver

```csharp
public class MySaveDriver : ISaveDriver
{
    public Task<SaveResultWithMessage> Save(string saveName, int version, 
        MetaData metaData, GameData gameData)
    {
        // 自定义保存逻辑
        byte[] bytes = SerializeGameData(gameData);
        File.WriteAllBytes(saveName + ".mysave", bytes);
        return Task.FromResult(new SaveResultWithMessage(true, ""));
    }
    
    public LoadData Load(string saveName)
    {
        // 自定义加载逻辑
        byte[] bytes = File.ReadAllBytes(saveName + ".mysave");
        return DeserializeGameData(bytes);
    }
}
```

---

## 常见问题

### Q: 为什么我的数据没有保存？

检查：
1. 类是否有 `[SaveableClass]` 或 `[SaveableRootClass]` 特性
2. 字段是否有 `[SaveableField]` 或 `[SaveableProperty]` 特性
3. 类型 ID 是否唯一（无冲突）

### Q: 存档后数据丢失？

常见原因：
1. **循环引用**：`A` 引用 `B`，`B` 也引用 `A`
2. **未注册的类型**：需要先注册自定义类型
3. **版本不兼容**：存档格式变化时需要处理迁移

### Q: 如何处理版本迁移？

```csharp
[SaveableClass(1)]
public class MyData
{
    [SaveableField(1)]
    public int OldField;  // 旧版本字段
    
    [SaveableField(2)]
    public int NewField;  // 新版本字段
    
    // 迁移逻辑
    public void MigrateFrom(object oldData)
    {
        if (oldData is OldMyData old)
        {
            NewField = old.OldField * 10;  // 数据转换
        }
    }
}
```

### Q: v1.3.0 和 v1.3.15 存档兼容吗？

不兼容。v1.3.15 使用全新的 TaleWorlds.SaveSystem，需要重新实现存档逻辑。

---

## 相关 API / Related API

- [SaveManager API](../api/save-system/SaveManager.md)
- [SaveAttributes API](../api/save-system/SaveAttributes.md)
- [AutoGeneratedSaveManager API](../api/save-system/AutoGeneratedSaveManager.md)
- [存档系统详解](../architecture/save-system.md)

---

## 下一节 / Next Section

- [常见模式](./common-patterns.md) - Bannerlord modding 最佳实践
- [战役系统](./campaign-system.md) - 了解 Campaign 世界
