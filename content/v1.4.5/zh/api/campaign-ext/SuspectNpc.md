---
title: "SuspectNpc"
description: "SuspectNpc 的自动生成类参考。"
---
# SuspectNpc

**Namespace:** SandBox.Issues
**Module:** SandBox.Issues
**Type:** `public struct SuspectNpc`
**Base:** 无
**File:** `Modules.SandBox/SandBox/SandBox.Issues/TheSpyPartyIssueQuestBehavior.cs`

## 概述

`SuspectNpc` 位于 `SandBox.Issues`，它通过这组公开成员把对应子系统的状态、行为或流程入口暴露给 mod 开发者。阅读时先看属性代表“它持有什么状态”，再看方法代表“它允许你做什么”。

## 心智模型

先从命名空间 `SandBox.Issues` 判断它属于哪层系统，再看公开方法：如果以 Get/Set 为主，它多半是状态对象；如果以 Create/Apply/Execute 为主，它更像服务或流程入口。

## 主要方法

### SuspectNpc
`public struct SuspectNpc(CharacterObject characterObject, bool hasHair, bool hasBigSword, bool withoutMarkings, bool hasBeard)`

**用途 / Purpose:** 执行此方法所描述的操作。

```csharp
// 先通过子系统 API 拿到 SuspectNpc 实例
SuspectNpc suspectNpc = ...;
var result = suspectNpc.SuspectNpc(characterObject, false, false, false, false);
```

### AutoGeneratedStaticCollectObjectsSuspectNpc
`public static void AutoGeneratedStaticCollectObjectsSuspectNpc(object o, List<object> collectedObjects)`

**用途 / Purpose:** 执行此方法所描述的操作。

```csharp
// 静态调用，不需要实例
SuspectNpc.AutoGeneratedStaticCollectObjectsSuspectNpc(o, collectedObjects);
```

## 使用示例

```csharp
// 通常从对应子系统 API 获取实例后调用
SuspectNpc suspectNpc = ...;
suspectNpc.SuspectNpc(characterObject, false, false, false, false);
```

## 参见

- [本区域目录](../)