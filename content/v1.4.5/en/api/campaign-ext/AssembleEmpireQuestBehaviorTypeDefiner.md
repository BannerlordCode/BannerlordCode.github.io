---
title: "AssembleEmpireQuestBehaviorTypeDefiner"
description: "Auto-generated class reference for AssembleEmpireQuestBehaviorTypeDefiner."
---
# AssembleEmpireQuestBehaviorTypeDefiner

**Namespace:** StoryMode.Quests.SecondPhase
**Module:** StoryMode.Quests
**Type:** `public class AssembleEmpireQuestBehaviorTypeDefiner : SaveableTypeDefiner`
**Base:** `SaveableTypeDefiner`
**File:** `Modules.StoryMode/StoryMode/StoryMode.Quests.SecondPhase/AssembleEmpireQuestBehavior.cs`

## Overview

`AssembleEmpireQuestBehaviorTypeDefiner` lives in `StoryMode.Quests.SecondPhase` and exposes the state, behavior, or workflow entry points of that subsystem to mod developers through its public members. Read its properties as “what state it owns” and its methods as “what actions it allows”.

## Mental Model

Start from namespace `StoryMode.Quests.SecondPhase` to place it in the stack, then inspect its public methods: if it mainly exposes Get/Set members, it is likely a state object; if it centers on Create/Apply/Execute verbs, it behaves more like a service or workflow entry point.

## Usage Example

```csharp
// Obtain an instance from the relevant subsystem API
AssembleEmpireQuestBehaviorTypeDefiner instance = ...;
```

## See Also

- [Area Index](../)