# v1.4.5 Clan Tier Model Wave

## Scope

Rewrite only these four existing v1.4.5 API stubs from the 1.4.5 source and verified call sites:

- `content/v1.4.5/zh/api/campaign/ClanTierModel.md`
- `content/v1.4.5/en/api/campaign/ClanTierModel.md`
- `content/v1.4.5/zh/api/campaign/DefaultClanTierModel.md`
- `content/v1.4.5/en/api/campaign/DefaultClanTierModel.md`

Do not modify navigation data, reports, unrelated pages, or existing dirty files. Do not commit.

## Acceptance

Each page must be a complete handwritten deep page: informative frontmatter and metadata; responsibility and lifecycle mental model; when to use and when not to use; clickable upstream/downstream/model/event/save dependencies; source-backed member behavior and timing; real model acquisition/use examples; crash/save/lifecycle risks; and reciprocal parent/sibling/related navigation. Chinese and English must express the same facts. No signature-to-prose generation, placeholder acquisition, `...`, `SomeValue`, `service = ...`, or signature-only sections.

## QA

After each author task, inspect the diff and run the scoped quality and handwritten classification checks. After both tasks, run npm tests, URL links, full quality, body-writer, navigation, section-tree, page-navigation, inventory, fresh bilingual coverage, and record the known R: Zola build limitation separately.

## Tasks

1. Rewrite `ClanTierModel` in Chinese and English from `ClanTierModel.cs`, `GameModels.cs`, `CampaignGameStarter.cs`, `SandBoxManager.cs`, `Clan.cs`, and call sites.
2. Rewrite `DefaultClanTierModel` in Chinese and English from `DefaultClanTierModel.cs`, its base contract, `Clan.cs`, model consumers, perks, and registration call sites.
