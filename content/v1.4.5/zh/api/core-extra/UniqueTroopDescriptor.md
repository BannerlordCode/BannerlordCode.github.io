---
title: "UniqueTroopDescriptor"
description: "UniqueTroopDescriptor 的自动生成类参考。"
---
# UniqueTroopDescriptor

**Namespace:** TaleWorlds.Core
**Module:** TaleWorlds.Core
**Type:** `public struct UniqueTroopDescriptor : IComparable<UniqueTroopDescriptor>, IEquatable<UniqueTroopDescriptor>, ISavedStruct`
**Base:** `IComparable<UniqueTroopDescriptor>`
**File:** `bin/TaleWorlds.Core/TaleWorlds.Core/UniqueTroopDescriptor.cs`

## 概述

`UniqueTroopDescriptor` 位于 `TaleWorlds.Core`，它通过这组公开成员把对应子系统的状态、行为或流程入口暴露给 mod 开发者。阅读时先看属性代表“它持有什么状态”，再看方法代表“它允许你做什么”。

## 心智模型

先从命名空间 `TaleWorlds.Core` 判断它属于哪层系统，再看公开方法：如果以 Get/Set 为主，它多半是状态对象；如果以 Create/Apply/Execute 为主，它更像服务或流程入口。

## 主要属性

| Name | Signature |
|------|-----------|
| `UniqueSeed` | `public int UniqueSeed { get; }` |

## 主要方法

### AutoGeneratedStaticCollectObjectsUniqueTroopDescriptor
`public static void AutoGeneratedStaticCollectObjectsUniqueTroopDescriptor(object o, List<object> collectedObjects)`

**用途 / Purpose:** 处理与 「auto generated static collect objects unique troop descriptor」 相关的逻辑。

```csharp
// 静态调用，不需要实例
UniqueTroopDescriptor.AutoGeneratedStaticCollectObjectsUniqueTroopDescriptor(o, collectedObjects);
```

### CompareTo
`public int CompareTo(UniqueTroopDescriptor other)`

**用途 / Purpose:** 将当前对象与传入实例比较大小或顺序。

```csharp
// 先通过子系统 API 拿到 UniqueTroopDescriptor 实例
UniqueTroopDescriptor uniqueTroopDescriptor = ...;
var result = uniqueTroopDescriptor.CompareTo(other);
```

### CompareTo
`public int CompareTo(int uniqueSeed)`

**用途 / Purpose:** 将当前对象与传入实例比较大小或顺序。

```csharp
// 先通过子系统 API 拿到 UniqueTroopDescriptor 实例
UniqueTroopDescriptor uniqueTroopDescriptor = ...;
var result = uniqueTroopDescriptor.CompareTo(0);
```

### ToString
`public override string ToString()`

**用途 / Purpose:** 返回当前对象的人类可读字符串表示。

```csharp
// 先通过子系统 API 拿到 UniqueTroopDescriptor 实例
UniqueTroopDescriptor uniqueTroopDescriptor = ...;
var result = uniqueTroopDescriptor.ToString();
```

### Equals
`public override bool Equals(object obj)`

**用途 / Purpose:** 比较当前对象与传入实例是否相等。

```csharp
// 先通过子系统 API 拿到 UniqueTroopDescriptor 实例
UniqueTroopDescriptor uniqueTroopDescriptor = ...;
var result = uniqueTroopDescriptor.Equals(obj);
```

### Equals
`public bool Equals(UniqueTroopDescriptor other)`

**用途 / Purpose:** 比较当前对象与传入实例是否相等。

```csharp
// 先通过子系统 API 拿到 UniqueTroopDescriptor 实例
UniqueTroopDescriptor uniqueTroopDescriptor = ...;
var result = uniqueTroopDescriptor.Equals(other);
```

### GetHashCode
`public override int GetHashCode()`

**用途 / Purpose:** 返回当前对象的哈希码，用于字典或哈希集合中的快速查找。

```csharp
// 先通过子系统 API 拿到 UniqueTroopDescriptor 实例
UniqueTroopDescriptor uniqueTroopDescriptor = ...;
var result = uniqueTroopDescriptor.GetHashCode();
```

## 使用示例

```csharp
UniqueTroopDescriptor.AutoGeneratedStaticCollectObjectsUniqueTroopDescriptor(o, collectedObjects);
```

## 参见

- [本区域目录](../)