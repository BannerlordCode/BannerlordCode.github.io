---
title: "ComponentInterfaces"
description: "Component-interface contracts used by systems."
---

## Up

- [Back to campaign index](../)
- [API Index](../../)
- [Version Home](../../../)

## Subsystem overview

This is the **ComponentInterfaces** subsystem inside **campaign index**. Classes in the same namespace tackle related concerns; pick a type, then open its page for methods.

Component-interface contracts used by systems.

## Classes

| Class | Purpose |
|------|---------|
| [`AccessDetails`](../AccessDetails/) | AccessDetails is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`AccessLevel`](../AccessLevel/) | AccessLevel is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`AccessLimitationReason`](../AccessLimitationReason/) | AccessLimitationReason is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`AccessMethod`](../AccessMethod/) | AccessMethod is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`AgeModel`](../AgeModel/) | AgeModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`AlleyModel`](../AlleyModel/) | AlleyModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`AllianceModel`](../AllianceModel/) | AllianceModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`ArmyManagementCalculationModel`](../ArmyManagementCalculationModel/) | ArmyManagementCalculationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`BanditDensityModel`](../BanditDensityModel/) | BanditDensityModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`BannerItemModel`](../BannerItemModel/) | BannerItemModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`BarterModel`](../BarterModel/) | BarterModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`BattleCaptainModel`](../BattleCaptainModel/) | BattleCaptainModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`BattleRewardModel`](../BattleRewardModel/) | BattleRewardModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`BodyPropertiesModel`](../BodyPropertiesModel/) | BodyPropertiesModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`BribeCalculationModel`](../BribeCalculationModel/) | BribeCalculationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`BuildingConstructionModel`](../BuildingConstructionModel/) | BuildingConstructionModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`BuildingEffectModel`](../BuildingEffectModel/) | BuildingEffectModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`BuildingModel`](../BuildingModel/) | BuildingModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`BuildingScoreCalculationModel`](../BuildingScoreCalculationModel/) | BuildingScoreCalculationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`CampaignShipDamageModel`](../CampaignShipDamageModel/) | CampaignShipDamageModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`CampaignShipParametersModel`](../CampaignShipParametersModel/) | CampaignShipParametersModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`CampaignTimeModel`](../CampaignTimeModel/) | CampaignTimeModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`CaravanModel`](../CaravanModel/) | CaravanModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`CharacterDevelopmentModel`](../CharacterDevelopmentModel/) | CharacterDevelopmentModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`CharacterStatsModel`](../CharacterStatsModel/) | CharacterStatsModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`ClanFinanceModel`](../ClanFinanceModel/) | ClanFinanceModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`ClanPoliticsModel`](../ClanPoliticsModel/) | ClanPoliticsModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`ClanTierModel`](../ClanTierModel/) | ClanTierModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`CombatSimulationModel`](../CombatSimulationModel/) | CombatSimulationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`CombatXpModel`](../CombatXpModel/) | CombatXpModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`CompanionHiringPriceCalculationModel`](../CompanionHiringPriceCalculationModel/) | CompanionHiringPriceCalculationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`CrimeModel`](../CrimeModel/) | CrimeModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`CutsceneSelectionModel`](../CutsceneSelectionModel/) | CutsceneSelectionModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`DailyTroopXpBonusModel`](../DailyTroopXpBonusModel/) | DailyTroopXpBonusModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`DefectionModel`](../DefectionModel/) | DefectionModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`DelayedTeleportationModel`](../DelayedTeleportationModel/) | DelayedTeleportationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`DifficultyModel`](../DifficultyModel/) | DifficultyModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`DiplomacyModel`](../DiplomacyModel/) | DiplomacyModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`DiplomacyStance`](../DiplomacyStance/) | DiplomacyStance is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`DisguiseDetectionModel`](../DisguiseDetectionModel/) | DisguiseDetectionModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`EmissaryModel`](../EmissaryModel/) | EmissaryModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`EncounterGameMenuModel`](../EncounterGameMenuModel/) | EncounterGameMenuModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`EncounterModel`](../EncounterModel/) | EncounterModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`EquipmentSelectionModel`](../EquipmentSelectionModel/) | EquipmentSelectionModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`ExecutionRelationModel`](../ExecutionRelationModel/) | ExecutionRelationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`FleetManagementModel`](../FleetManagementModel/) | FleetManagementModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`GenericXpModel`](../GenericXpModel/) | GenericXpModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`HeirSelectionCalculationModel`](../HeirSelectionCalculationModel/) | HeirSelectionCalculationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`HeroAgentLocationModel`](../HeroAgentLocationModel/) | HeroAgentLocationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`HeroCreationModel`](../HeroCreationModel/) | HeroCreationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`HeroDeathProbabilityCalculationModel`](../HeroDeathProbabilityCalculationModel/) | HeroDeathProbabilityCalculationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`HeroLocationDetail`](../HeroLocationDetail/) | HeroLocationDetail is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`HideoutModel`](../HideoutModel/) | HideoutModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`INavigationCache`](../INavigationCache/) | INavigationCache is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`IncidentModel`](../IncidentModel/) | IncidentModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`InformationRestrictionModel`](../InformationRestrictionModel/) | InformationRestrictionModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`InventoryCapacityModel`](../InventoryCapacityModel/) | InventoryCapacityModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`IssueModel`](../IssueModel/) | IssueModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`ItemDiscardModel`](../ItemDiscardModel/) | ItemDiscardModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`KingdomCreationModel`](../KingdomCreationModel/) | KingdomCreationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`KingdomDecisionPermissionModel`](../KingdomDecisionPermissionModel/) | KingdomDecisionPermissionModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`LimitedAccessSolution`](../LimitedAccessSolution/) | LimitedAccessSolution is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`LocationModel`](../LocationModel/) | LocationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`MapDistanceModel`](../MapDistanceModel/) | MapDistanceModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`MapTrackModel`](../MapTrackModel/) | MapTrackModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`MapVisibilityModel`](../MapVisibilityModel/) | MapVisibilityModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`MapWeatherModel`](../MapWeatherModel/) | MapWeatherModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`MarriageModel`](../MarriageModel/) | MarriageModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`MilitaryPowerModel`](../MilitaryPowerModel/) | MilitaryPowerModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`MinorFactionsModel`](../MinorFactionsModel/) | MinorFactionsModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`MissionTypeEnum`](../MissionTypeEnum/) | MissionTypeEnum is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`MobilePartyAIModel`](../MobilePartyAIModel/) | MobilePartyAIModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`MobilePartyFoodConsumptionModel`](../MobilePartyFoodConsumptionModel/) | MobilePartyFoodConsumptionModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`MobilePartyMoraleModel`](../MobilePartyMoraleModel/) | MobilePartyMoraleModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`NotablePowerModel`](../NotablePowerModel/) | NotablePowerModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`NotableSpawnModel`](../NotableSpawnModel/) | NotableSpawnModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartyDesertionModel`](../PartyDesertionModel/) | PartyDesertionModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartyFoodBuyingModel`](../PartyFoodBuyingModel/) | PartyFoodBuyingModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartyHealingModel`](../PartyHealingModel/) | PartyHealingModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartyImpairmentModel`](../PartyImpairmentModel/) | PartyImpairmentModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartyMoraleModel`](../PartyMoraleModel/) | PartyMoraleModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartyNavigationModel`](../PartyNavigationModel/) | PartyNavigationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartyShipLimitModel`](../PartyShipLimitModel/) | PartyShipLimitModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartySizeLimitModel`](../PartySizeLimitModel/) | PartySizeLimitModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartySpeedModel`](../PartySpeedModel/) | PartySpeedModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartyTradeModel`](../PartyTradeModel/) | PartyTradeModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartyTrainingModel`](../PartyTrainingModel/) | PartyTrainingModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartyTransitionModel`](../PartyTransitionModel/) | PartyTransitionModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartyTroopUpgradeModel`](../PartyTroopUpgradeModel/) | PartyTroopUpgradeModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PartyWageModel`](../PartyWageModel/) | PartyWageModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PaymentMethod`](../PaymentMethod/) | PaymentMethod is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`PersuasionModel`](../PersuasionModel/) | PersuasionModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PlayerProgressionModel`](../PlayerProgressionModel/) | PlayerProgressionModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PregnancyModel`](../PregnancyModel/) | PregnancyModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PreliminaryActionObligation`](../PreliminaryActionObligation/) | PreliminaryActionObligation is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`PreliminaryActionType`](../PreliminaryActionType/) | PreliminaryActionType is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`PrisonBreakModel`](../PrisonBreakModel/) | PrisonBreakModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PrisonerDonationModel`](../PrisonerDonationModel/) | PrisonerDonationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`PrisonerRecruitmentCalculationModel`](../PrisonerRecruitmentCalculationModel/) | PrisonerRecruitmentCalculationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`RaidModel`](../RaidModel/) | RaidModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`RansomValueCalculationModel`](../RansomValueCalculationModel/) | RansomValueCalculationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`RomanceModel`](../RomanceModel/) | RomanceModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SceneModel`](../SceneModel/) | SceneModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SettlementAccessModel`](../SettlementAccessModel/) | SettlementAccessModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SettlementAction`](../SettlementAction/) | SettlementAction is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`SettlementEconomyModel`](../SettlementEconomyModel/) | SettlementEconomyModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SettlementFoodModel`](../SettlementFoodModel/) | SettlementFoodModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SettlementGarrisonModel`](../SettlementGarrisonModel/) | SettlementGarrisonModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SettlementLoyaltyModel`](../SettlementLoyaltyModel/) | SettlementLoyaltyModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SettlementMenuOverlayModel`](../SettlementMenuOverlayModel/) | SettlementMenuOverlayModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SettlementMilitiaModel`](../SettlementMilitiaModel/) | SettlementMilitiaModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SettlementPatrolModel`](../SettlementPatrolModel/) | SettlementPatrolModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SettlementProsperityModel`](../SettlementProsperityModel/) | SettlementProsperityModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SettlementSecurityModel`](../SettlementSecurityModel/) | SettlementSecurityModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SettlementTaxModel`](../SettlementTaxModel/) | SettlementTaxModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SettlementValueModel`](../SettlementValueModel/) | SettlementValueModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`ShipCostModel`](../ShipCostModel/) | ShipCostModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`ShipStatModel`](../ShipStatModel/) | ShipStatModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SiegeAction`](../SiegeAction/) | SiegeAction is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`SiegeAftermathModel`](../SiegeAftermathModel/) | SiegeAftermathModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SiegeEventModel`](../SiegeEventModel/) | SiegeEventModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SiegeLordsHallFightModel`](../SiegeLordsHallFightModel/) | SiegeLordsHallFightModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SiegeStrategyActionModel`](../SiegeStrategyActionModel/) | SiegeStrategyActionModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`SmithingModel`](../SmithingModel/) | SmithingModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`TargetScoreCalculatingModel`](../TargetScoreCalculatingModel/) | TargetScoreCalculatingModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`TavernMercenaryTroopsModel`](../TavernMercenaryTroopsModel/) | TavernMercenaryTroopsModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`TournamentModel`](../TournamentModel/) | TournamentModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`TradeAgreementModel`](../TradeAgreementModel/) | TradeAgreementModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`TradeItemPriceFactorModel`](../TradeItemPriceFactorModel/) | TradeItemPriceFactorModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`TroopSacrificeModel`](../TroopSacrificeModel/) | TroopSacrificeModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`TroopSupplierProbabilityModel`](../TroopSupplierProbabilityModel/) | TroopSupplierProbabilityModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`ValuationModel`](../ValuationModel/) | ValuationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`VassalRewardsModel`](../VassalRewardsModel/) | VassalRewardsModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`VillageProductionCalculatorModel`](../VillageProductionCalculatorModel/) | VillageProductionCalculatorModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`VillageTradeModel`](../VillageTradeModel/) | VillageTradeModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`VoiceOverModel`](../VoiceOverModel/) | VoiceOverModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`VolunteerModel`](../VolunteerModel/) | VolunteerModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`WallHitPointCalculationModel`](../WallHitPointCalculationModel/) | WallHitPointCalculationModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |
| [`WeatherEvent`](../WeatherEvent/) | WeatherEvent is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`WeatherEventEffectOnTerrain`](../WeatherEventEffectOnTerrain/) | WeatherEventEffectOnTerrain is a public type in TaleWorlds.CampaignSystem.ComponentInterfaces. |
| [`WorkshopModel`](../WorkshopModel/) | WorkshopModel is a rule model in TaleWorlds.CampaignSystem.ComponentInterfaces; it defines how a subsystem computes things, and modders usually customize it by subclassing or replacement. |

## Other subsystems in this area

- [Root](./../root/)
- [AgentOrigins](./../agentorigins/)
- [BarterSystem](./../bartersystem/)
- [CampaignBehaviors](./../campaignbehaviors/)
- [CharacterCreationContent](./../charactercreationcontent/)
- [CharacterDevelopment](./../characterdevelopment/)
- [Characters](./../characters/)
- [Conversation](./../conversation/)
- [Election](./../election/)
- [Encounters](./../encounters/)
- [Encyclopedia](./../encyclopedia/)
- [Extensions](./../extensions/)
- [GameComponents](./../gamecomponents/)
- [GameMenus](./../gamemenus/)
- [GameState](./../gamestate/)
- [Heroes](./../heroes/)
- [Inventory](./../inventory/)
- [Issues](./../issues/)
- [LogEntries](./../logentries/)
- [Map](./../map/)
- [MapEvents](./../mapevents/)
- [MapNotificationTypes](./../mapnotificationtypes/)
- [Naval](./../naval/)
- [Parties](./../parties/)
- [Party](./../party/)
- [Quests](./../quests/)
- [Roster](./../roster/)
- [SaveCompability](./../savecompability/)
- [SceneInformationPopupTypes](./../sceneinformationpopuptypes/)
- [Settlements](./../settlements/)
- [Siege](./../siege/)
- [TournamentGames](./../tournamentgames/)
