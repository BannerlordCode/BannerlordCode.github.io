# v1.4.5 Runtime Foundation Wave Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the eight v1.4.5 runtime-foundation stubs with source-backed bilingual pages for `CampaignObjectManager`, `PartyComponent`, `CampaignEventDispatcher`, and `CampaignPeriodicEventManager`.

**Architecture:** Keep the existing `api/campaign` routes and write each language from the same v1.4.5 source facts. Explain ownership, lifecycle, public read/query boundaries, event/tick sequencing, real acquisition paths, save behavior, and failure risks; use existing clean-URL relative links and do not alter navigation or unrelated waves.

**Tech Stack:** Zola Markdown, Node.js handwritten-policy and documentation gates, Bannerlord v1.4.5 C# source under `bannerlord-1.4.5/Bannerlord.Source`.

## Global Constraints

- Modify only the eight target pages and this wave's plan/evidence artifacts.
- All body prose is handwritten from source and bounded call sites; no signature-to-prose or bulk body generator.
- Every page must contain metadata, an informative overview, a real mental model, clickable dependencies, real C# acquisition/use examples, risks, and reciprocal navigation.
- Preserve all existing dirty/concurrent files; do not reset, clean, stash, checkout, commit, or push.
- Keep the global H0-H10 goal `active`; this wave is not final completion because R1 gaps and build evidence remain.

---

### Task 1: Author `CampaignObjectManager` and `PartyComponent`

**Files:**
- Modify: `content/v1.4.5/zh/api/campaign/CampaignObjectManager.md`
- Modify: `content/v1.4.5/en/api/campaign/CampaignObjectManager.md`
- Modify: `content/v1.4.5/zh/api/campaign/PartyComponent.md`
- Modify: `content/v1.4.5/en/api/campaign/PartyComponent.md`

**Source facts to preserve:**
- `Campaign.Current.CampaignObjectManager` is created by `Campaign`, owns read-only lists for heroes, factions, settlements, and party categories, updates registrations as objects are created/removed or change state, and exposes generic `Find`, `FindFirst`, `FindAll`, and `FindNextUniqueStringId`.
- Its object collections and settlement list participate in save collection; callers must not mutate the returned read-only views or manufacture IDs.
- `PartyComponent` is abstract and attached to a `MobileParty`; `Create`, `Initialize`, and `Finish` control its internal lifecycle. `Party`, `MobileParty`, owner, name, home settlement, leader, wage limit, navigation capability, banner, leader change, and finalization are component contracts.
- Concrete component factories such as `CaravanPartyComponent`, `LordPartyComponent`, `GarrisonPartyComponent`, and `CustomPartyComponent` create the component and party together; callers should not instantiate the abstract base or write component-owned state directly.

- [ ] Write both pages with real examples using `Campaign.Current.CampaignObjectManager` and an existing `MobileParty` component.
- [ ] Include links to `Campaign`, `Hero`, `Clan`, `Kingdom`, `Settlement`, `MobileParty`, `PartyBase`, and at least two concrete component pages where relevant.
- [ ] Verify both pages classify as `deep_pass` and contain no forbidden placeholder acquisition.

### Task 2: Author `CampaignEventDispatcher` and `CampaignPeriodicEventManager`

**Files:**
- Modify: `content/v1.4.5/zh/api/campaign/CampaignEventDispatcher.md`
- Modify: `content/v1.4.5/en/api/campaign/CampaignEventDispatcher.md`
- Modify: `content/v1.4.5/zh/api/campaign/CampaignPeriodicEventManager.md`
- Modify: `content/v1.4.5/en/api/campaign/CampaignPeriodicEventManager.md`

**Source facts to preserve:**
- `CampaignEventDispatcher.Instance` is the current campaign's internal receiver fan-out. It forwards callbacks such as `OnHeroJoinedParty`, settlement entry, game-load, hourly/daily, and AI ticks to registered receivers and removes a listener object across receivers.
- Mod code normally subscribes to `CampaignEvents` in `CampaignBehaviorBase.RegisterEvents`; it should not call the dispatcher directly or treat it as a public event-registration API.
- `CampaignPeriodicEventManager` initializes typed tickers over current `MobileParty`, `Settlement`, `Town`, `Hero`, and `Clan` collections; it advances them from `Campaign.Tick`, with partial AI ticks and custom periodic events at defined phases.
- `CreatePeriodicEvent` creates an `MBCampaignEvent` and adds it to `Campaign.Current.CustomPeriodicCampaignEvents`; deleted events are removed during signaling, and the custom event list is saved with the campaign.

- [ ] Write both pages with real `CampaignEvents` subscription and `CampaignPeriodicEventManager.CreatePeriodicEvent` examples using real campaign objects/types.
- [ ] Explain order/timing, listener cleanup, save/load and deletion behavior, and why direct dispatcher/ticker manipulation is unsafe.
- [ ] Verify both pages classify as `deep_pass` and link to `CampaignEvents`, `CampaignBehaviorBase`, `MBCampaignEvent`, and relevant entity pages.

### Task 3: Scoped verification and reviewer gate

**Files:**
- Create: `.superpowers/sdd/2026-08-04-v1.4.5-runtime-foundation-wave/progress.md`
- Evidence outputs: only files recorded in the ledger, with temporary generated reports under `.tmp` while the tree is active.

- [ ] Run scoped classifier, per-page quality, forbidden-token, bilingual heading/parity, and v1.4.5 URL checks.
- [ ] If all eight pages pass the scoped mechanical gate, dispatch one independent read-only reviewer; otherwise fix only these eight pages and repeat the scoped checks.
- [ ] Record exact commands, outputs, file boundary, reviewer verdict/findings, and unresolved global R1/build limitations in the ledger.
- [ ] Recheck `git status --short` and confirm no unrelated path changed; leave the global goal active.

## Test Plan

### Objective

Prove that the eight target pages are handwritten deep pages with accurate source-backed content, valid clean-URL links, bilingual structural parity, and no forbidden fake examples.

### Test Cases

1. **Classification:** all eight target files report `status=deep_pass`.
2. **Quality:** each target page reports zero blockers and zero target-page warnings.
3. **Content rejection scan:** target pages contain no `SomeValue`, `service = ...`, empty ellipsis example, or generator boilerplate.
4. **Parity:** Chinese and English pages have matching required section structure and the same public behavior claims.
5. **URL audit:** scoped v1.4.5 audit reports zero broken target-page links; unrelated pre-existing findings are recorded separately if present.
6. **Boundary:** `git status --short` shows changes only in the eight pages and this plan/ledger/evidence set attributable to this wave.

### Success Criteria

All six test cases pass for this wave. Passing this wave does not close the global H0-H10 goal, which remains active until the broader coverage, runtime, and supported-build requirements are met.
