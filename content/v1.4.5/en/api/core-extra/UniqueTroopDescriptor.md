---
title: "UniqueTroopDescriptor"
---
<!-- BEGIN BREADCRUMB -->
**Home** → **API Index** → **Area** → `UniqueTroopDescriptor`
- [← Area / Back to core-extra](./)
- [↑ API Index](../)
- [🏠 Home v1.4.5](../../)
- [⭐ Version Architecture](../../architecture/)
<!-- END BREADCRUMB -->
# UniqueTroopDescriptor

**Namespace:** TaleWorlds.Core
**Module:** TaleWorlds.Core
**Type:** `public struct UniqueTroopDescriptor : IComparable<UniqueTroopDescriptor>, IEquatable<UniqueTroopDescriptor>, ISavedStruct`
**Base:** `IComparable<UniqueTroopDescriptor>`
**File:** `Bannerlord.Source/bin/TaleWorlds.Core/TaleWorlds.Core/UniqueTroopDescriptor.cs`

## Overview

`UniqueTroopDescriptor` lives in `TaleWorlds.Core` and exposes the state, behavior, or workflow entry points of that subsystem to mod developers through its public members. Read its properties as “what state it owns” and its methods as “what actions it allows”.

## Mental Model

Start from namespace `TaleWorlds.Core` to place it in the stack, then inspect its public methods: if it mainly exposes Get/Set members, it is likely a state object; if it centers on Create/Apply/Execute verbs, it behaves more like a service or workflow entry point.

## Key Properties

| Name | Signature |
|------|-----------|
| `UniqueSeed` | `public int UniqueSeed { get; }` |

## Key Methods

### AutoGeneratedStaticCollectObjectsUniqueTroopDescriptor
`public static void AutoGeneratedStaticCollectObjectsUniqueTroopDescriptor(object o, List<object> collectedObjects)`

**Purpose:** Handles logic related to `auto generated static collect objects unique troop descriptor`.

### CompareTo
`public int CompareTo(UniqueTroopDescriptor other)`

**Purpose:** Handles logic related to `compare to`.

### CompareTo
`public int CompareTo(int uniqueSeed)`

**Purpose:** Handles logic related to `compare to`.

### ToString
`public override string ToString()`

**Purpose:** Handles logic related to `to string`.

### Equals
`public override bool Equals(object obj)`

**Purpose:** Handles logic related to `equals`.

### Equals
`public bool Equals(UniqueTroopDescriptor other)`

**Purpose:** Handles logic related to `equals`.

### GetHashCode
`public override int GetHashCode()`

**Purpose:** Gets the current value of `hash code`.

## Usage Example

```csharp
UniqueTroopDescriptor.AutoGeneratedStaticCollectObjectsUniqueTroopDescriptor(o, collectedObjects);
```

## See Also

- [Complete Class Catalog](../catalog)