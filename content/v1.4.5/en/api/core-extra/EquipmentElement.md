---
title: "EquipmentElement"
description: "Auto-generated class reference for EquipmentElement."
---
# EquipmentElement

**Namespace:** TaleWorlds.Core
**Module:** TaleWorlds.Core
**Type:** `public struct EquipmentElement : ISerializableObject, ISavedStruct`
**Base:** `ISerializableObject`
**File:** `bin/TaleWorlds.Core/TaleWorlds.Core/EquipmentElement.cs`

## Overview

`EquipmentElement` lives in `TaleWorlds.Core` and exposes the state, behavior, or workflow entry points of that subsystem to mod developers through its public members. Read its properties as “what state it owns” and its methods as “what actions it allows”.

## Mental Model

Start from namespace `TaleWorlds.Core` to place it in the stack, then inspect its public methods: if it mainly exposes Get/Set members, it is likely a state object; if it centers on Create/Apply/Execute verbs, it behaves more like a service or workflow entry point.

## Key Properties

| Name | Signature |
|------|-----------|
| `Item` | `public ItemObject Item { get; }` |
| `ItemModifier` | `public ItemModifier ItemModifier { get; }` |
| `IsQuestItem` | `public bool IsQuestItem { get; }` |
| `IsVisualEmpty` | `public bool IsVisualEmpty { get; }` |
| `ItemValue` | `public int ItemValue { get; }` |
| `Weight` | `public float Weight { get; }` |

## Key Methods

### AutoGeneratedStaticCollectObjectsEquipmentElement
`public static void AutoGeneratedStaticCollectObjectsEquipmentElement(object o, List<object> collectedObjects)`

**Purpose:** **Purpose:** Executes the AutoGeneratedStaticCollectObjectsEquipmentElement logic.

```csharp
// Static call; no instance required
EquipmentElement.AutoGeneratedStaticCollectObjectsEquipmentElement(o, collectedObjects);
```

### SetModifier
`public void SetModifier(ItemModifier itemModifier)`

**Purpose:** **Purpose:** Assigns a new value to modifier and updates the object's internal state.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
equipmentElement.SetModifier(itemModifier);
```

### Clear
`public void Clear()`

**Purpose:** **Purpose:** Removes all content from the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
equipmentElement.Clear();
```

### GetHashCode
`public override int GetHashCode()`

**Purpose:** **Purpose:** Returns a hash code for the this instance, used for fast lookup in dictionaries and hash sets.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetHashCode();
```

### ToString
`public override string ToString()`

**Purpose:** **Purpose:** Returns a human-readable string representation of the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.ToString();
```

### GetModifiedHeadArmor
`public int GetModifiedHeadArmor()`

**Purpose:** **Purpose:** Reads and returns the modified head armor value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedHeadArmor();
```

### GetModifiedBodyArmor
`public int GetModifiedBodyArmor()`

**Purpose:** **Purpose:** Reads and returns the modified body armor value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedBodyArmor();
```

### GetModifiedMountBodyArmor
`public int GetModifiedMountBodyArmor()`

**Purpose:** **Purpose:** Reads and returns the modified mount body armor value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedMountBodyArmor();
```

### GetModifiedLegArmor
`public int GetModifiedLegArmor()`

**Purpose:** **Purpose:** Reads and returns the modified leg armor value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedLegArmor();
```

### GetModifiedArmArmor
`public int GetModifiedArmArmor()`

**Purpose:** **Purpose:** Reads and returns the modified arm armor value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedArmArmor();
```

### GetModifiedStealthFactor
`public int GetModifiedStealthFactor()`

**Purpose:** **Purpose:** Reads and returns the modified stealth factor value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedStealthFactor();
```

### GetModifiedMaximumHitPointsForUsage
`public short GetModifiedMaximumHitPointsForUsage(int usageIndex)`

**Purpose:** **Purpose:** Reads and returns the modified maximum hit points for usage value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedMaximumHitPointsForUsage(0);
```

### GetModifiedItemName
`public TextObject GetModifiedItemName()`

**Purpose:** **Purpose:** Reads and returns the modified item name value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedItemName();
```

### GetModifiedThrustDamageForUsage
`public int GetModifiedThrustDamageForUsage(int usageIndex)`

**Purpose:** **Purpose:** Reads and returns the modified thrust damage for usage value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedThrustDamageForUsage(0);
```

### GetModifiedSwingDamageForUsage
`public int GetModifiedSwingDamageForUsage(int usageIndex)`

**Purpose:** **Purpose:** Reads and returns the modified swing damage for usage value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedSwingDamageForUsage(0);
```

### GetModifiedMissileDamageForUsage
`public int GetModifiedMissileDamageForUsage(int usageIndex)`

**Purpose:** **Purpose:** Reads and returns the modified missile damage for usage value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedMissileDamageForUsage(0);
```

### GetModifiedThrustSpeedForUsage
`public int GetModifiedThrustSpeedForUsage(int usageIndex)`

**Purpose:** **Purpose:** Reads and returns the modified thrust speed for usage value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedThrustSpeedForUsage(0);
```

### GetModifiedSwingSpeedForUsage
`public int GetModifiedSwingSpeedForUsage(int usageIndex)`

**Purpose:** **Purpose:** Reads and returns the modified swing speed for usage value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedSwingSpeedForUsage(0);
```

### GetModifiedMissileSpeedForUsage
`public int GetModifiedMissileSpeedForUsage(int usageIndex)`

**Purpose:** **Purpose:** Reads and returns the modified missile speed for usage value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedMissileSpeedForUsage(0);
```

### GetModifiedHandlingForUsage
`public int GetModifiedHandlingForUsage(int usageIndex)`

**Purpose:** **Purpose:** Reads and returns the modified handling for usage value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedHandlingForUsage(0);
```

### GetModifiedStackCountForUsage
`public short GetModifiedStackCountForUsage(int usageIndex)`

**Purpose:** **Purpose:** Reads and returns the modified stack count for usage value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedStackCountForUsage(0);
```

### GetBaseValue
`public int GetBaseValue()`

**Purpose:** **Purpose:** Reads and returns the base value value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetBaseValue();
```

### IsEqualTo
`public bool IsEqualTo(EquipmentElement other)`

**Purpose:** **Purpose:** Determines whether the this instance is in the equal to state or condition.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.IsEqualTo(other);
```

### Equals
`public override bool Equals(object obj)`

**Purpose:** **Purpose:** Compares the this instance with the supplied instance for equality.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.Equals(obj);
```

### Equals
`public bool Equals(ItemRosterElement other)`

**Purpose:** **Purpose:** Compares the this instance with the supplied instance for equality.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.Equals(other);
```

### GetEquipmentElementWeight
`public float GetEquipmentElementWeight()`

**Purpose:** **Purpose:** Reads and returns the equipment element weight value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetEquipmentElementWeight();
```

### IsInvalid
`public bool IsInvalid()`

**Purpose:** **Purpose:** Determines whether the this instance is in the invalid state or condition.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.IsInvalid();
```

### GetModifiedMountManeuver
`public int GetModifiedMountManeuver(in EquipmentElement harness)`

**Purpose:** **Purpose:** Reads and returns the modified mount maneuver value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedMountManeuver(harness);
```

### GetModifiedMountSpeed
`public int GetModifiedMountSpeed(in EquipmentElement harness)`

**Purpose:** **Purpose:** Reads and returns the modified mount speed value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedMountSpeed(harness);
```

### GetModifiedMountCharge
`public int GetModifiedMountCharge(in EquipmentElement harness)`

**Purpose:** **Purpose:** Reads and returns the modified mount charge value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedMountCharge(harness);
```

### GetModifiedMountHitPoints
`public int GetModifiedMountHitPoints()`

**Purpose:** **Purpose:** Reads and returns the modified mount hit points value held by the this instance.

```csharp
// Obtain an instance of EquipmentElement from the subsystem API first
EquipmentElement equipmentElement = ...;
var result = equipmentElement.GetModifiedMountHitPoints();
```

## Usage Example

```csharp
EquipmentElement.AutoGeneratedStaticCollectObjectsEquipmentElement(o, collectedObjects);
```

## See Also

- [Area Index](../)