---
title: "StaticBodyProperties"
description: "Auto-generated class reference for StaticBodyProperties."
---
# StaticBodyProperties

**Namespace:** TaleWorlds.Core
**Module:** TaleWorlds.Core
**Type:** `public struct StaticBodyProperties : ISerializableObject`
**Base:** `ISerializableObject`
**File:** `TaleWorlds.Core/StaticBodyProperties.cs`

## Overview

`StaticBodyProperties` lives in `TaleWorlds.Core` and exposes the state, behavior, or workflow entry points of that subsystem to mod developers through its public members. Read its properties as “what state it owns” and its methods as “what actions it allows”.

## Mental Model

Start from namespace `TaleWorlds.Core` to place it in the stack, then inspect its public methods: if it mainly exposes Get/Set members, it is likely a state object; if it centers on Create/Apply/Execute verbs, it behaves more like a service or workflow entry point.

## Key Properties

| Name | Signature |
|------|-----------|
| `KeyPart1` | `public ulong KeyPart1 { get; }` |
| `KeyPart2` | `public ulong KeyPart2 { get; }` |
| `KeyPart3` | `public ulong KeyPart3 { get; }` |
| `KeyPart4` | `public ulong KeyPart4 { get; }` |
| `KeyPart5` | `public ulong KeyPart5 { get; }` |
| `KeyPart6` | `public ulong KeyPart6 { get; }` |
| `KeyPart7` | `public ulong KeyPart7 { get; }` |
| `KeyPart8` | `public ulong KeyPart8 { get; }` |

## Key Methods

### AutoGeneratedStaticCollectObjectsStaticBodyProperties
`public static void AutoGeneratedStaticCollectObjectsStaticBodyProperties(object o, List<object> collectedObjects)`

**Purpose:** Executes the AutoGeneratedStaticCollectObjectsStaticBodyProperties logic.

```csharp
// Static call; no instance required
StaticBodyProperties.AutoGeneratedStaticCollectObjectsStaticBodyProperties(o, collectedObjects);
```

### FromXmlNode
`public static bool FromXmlNode(XmlNode node, out StaticBodyProperties staticBodyProperties)`

**Purpose:** Executes the FromXmlNode logic.

```csharp
// Static call; no instance required
StaticBodyProperties.FromXmlNode(node, staticBodyProperties);
```

### GetHashCode
`public override int GetHashCode()`

**Purpose:** Returns a hash code for the this instance, used for fast lookup in dictionaries and hash sets.

```csharp
// Obtain an instance of StaticBodyProperties from the subsystem API first
StaticBodyProperties staticBodyProperties = ...;
var result = staticBodyProperties.GetHashCode();
```

### Equals
`public override bool Equals(object obj)`

**Purpose:** Compares the this instance with the supplied instance for equality.

```csharp
// Obtain an instance of StaticBodyProperties from the subsystem API first
StaticBodyProperties staticBodyProperties = ...;
var result = staticBodyProperties.Equals(obj);
```

### ToString
`public override string ToString()`

**Purpose:** Returns a human-readable string representation of the this instance.

```csharp
// Obtain an instance of StaticBodyProperties from the subsystem API first
StaticBodyProperties staticBodyProperties = ...;
var result = staticBodyProperties.ToString();
```

### GetRandomStaticBodyProperties
`public static StaticBodyProperties GetRandomStaticBodyProperties()`

**Purpose:** Reads and returns the random static body properties value held by the this instance.

```csharp
// Static call; no instance required
StaticBodyProperties.GetRandomStaticBodyProperties();
```

## Usage Example

```csharp
StaticBodyProperties.AutoGeneratedStaticCollectObjectsStaticBodyProperties(o, collectedObjects);
```

## See Also

- [Area Index](../)